// Deliberately blank: the dashboard's first real surface (auth + moderation, Module 1)
// lands at W2.7 (docs/spec/17). Tokens/theming arrive from @agbc/shared at W0.7.
export default function Home() {
  return <main className="flex-1" />;
}
