# W4.9 · Audio shelf and player (slice plan)

**STATUS: planned 2026-09-06, nothing built.** Five slices, dashboard first, each its own
PR. Slices 2, 3 and 4 change screens and therefore start with frames in
`docs/spec/design/mockups/entry-flow.html` that Ayo approves BEFORE any screen code (the
mockup-first rule in the project CLAUDE.md). Tick the boxes in this file as slices land, and
**delete this file when the last slice lands**, per the W3.1/W3.3/W4.1 convention: what it
decided moves into `08`, `05`, `17` §4, `02` §Storage and each migration's own header.

Refs: `08` (media architecture, background audio, the YouTube box), `02` §Storage
(`sermon-audio`), `05` (tokens, the `.pl-scrub` and `.seg` patterns), `17` §4 (the audio
shelf), `21` §4 (verification matrix), `18` (launch checklist, the audio owner line),
`docs/runbooks/store-submission.md` §SUBMISSION RECORD (what the first upload found).

---

## 1. Why this item exists

The first sermon audio ever put on production went up on 2026-09-05, to film the Play
Console's foreground-service declaration. One evening of real use found six things, and
they are this item:

1. **The manage screen crashed for any message with audio** (Sentry AGBC-DASHBOARD-2):
   `/sermon-audio/[id]` is a Server Component handing a copy bundle with three functions to
   the client-side `ImageField`. Fixed on `fix/dashboard-manage-artwork-boundary` (PR
   pending as this is written). Not part of this item, but the reason its manage screen
   had never been seen on production.
2. **The upload limit the dashboard states is not the one production enforces.** The
   uploader, the copy and the bucket all say 150 MB; the Supabase Free plan refuses anything
   over 50 MB at the storage layer, and the failure came back as "check your connection".
   A 57 MB m4a failed at 22 MB with no true word said about why.
3. **The format advice was a suggestion, and the file that arrived was m4a.** `08` and the
   shelf's own note ask for 64-96 kbps mono MP3. The picker accepted m4a and aac anyway.
4. **Audio-only messages have no home in Watch.** The tab is a hero, Recent messages and
   Recent live streams. An audio-only message is the hero while it is newest, then one row
   among videos, then gone from every rail. Search finds it (title, speaker, series), but
   nobody browsing for something to listen to can see the set.
5. **The player cannot seek except by 15 s, and stops at 1.5x.** Going back to the 30th
   minute of a 96-minute message is 120 taps. A member who listens at 2x cannot.
6. **Leaving the player stops the listening.** The player is created by the sermon
   screen's own hook (`useAudioPlayer` in `useSermonAudio.ts`, line 57) and paused on
   blur (`useFocusEffect`, line 178), so a member who taps back to read the verse of the
   day or check an event loses the message. Backgrounding the whole app keeps it playing;
   staying in the app does not. `08` promises "listen while driving" and says nothing
   about listening while browsing, and the mockup has no frame for it.

## 2. Decisions taken with Ayo (2026-09-06)

- **Audio-only messages get editable facts: title, speaker, series, date preached.** A
  typo can currently only be fixed in the database. Synced YouTube messages stay read-only,
  because the nightly sync owns their facts and would overwrite an edit. **No delete.**
  Removal of an audio-only message stays refused as today (`removeAudio`, reason
  `audio_only`): a message that is nothing at all is not a state we want reachable from a
  button. If a wrong upload has to go, the path is Replace.
- **MP3 only, enforced.** The picker, the mint and the byte check accept `.mp3` /
  `audio/mpeg` and nothing else; the copy says to convert first and how. Decided over
  advise-only because tonight's m4a would then have been refused in the browser with a true
  reason, instead of failing mid-upload with a false one.
- **The size cap is the plan's cap.** `MAX_AUDIO_BYTES` becomes 50 MB and the copy says
  50 MB, with a comment naming the Free-plan limit as the source, so the browser refuses
  before a byte is sent and the words are true. If the project moves to Pro, this constant
  and its copy are the whole change.
- **Watch gets a segmented control: Video / Audio.** Chosen over a third rail and over a
  badge inside Recent messages. The Audio segment lists audio-only messages newest first.
  Search is unchanged in shape (it already matches audio-only messages); its rows show the
  audio cover, which `features/watch/artwork.ts` already decides.
- **Speed goes to 2x, not 3x, and the reason is native.** The installed expo-audio (57.0.3)
  clamps the rate on Android: `AudioPlayer.kt:184`, `rate.coerceIn(0.1f, 2.0f)`. iOS passes
  the rate to AVPlayer unclamped (`AudioModule.swift:223`). Shipping 3x would mean a
  patched native module and an EAS rebuild for a control that would then behave
  differently per platform. Ayo's ask was "3x if possible, otherwise 2x"; the answer is 2x.
  Steps: 1, 1.25, 1.5, 2. Revisit if expo-audio lifts the clamp (check the same line).
- **The scrub bar becomes a seek control**: drag anywhere, tap anywhere, with a knob so it
  looks like one. Built on `PanResponder`, not a native slider, so no dev-client rebuild is
  needed (the dev-client native fence in CLAUDE.md).
- **Listening survives navigation, and a mini player is the way back.** The player moves
  above the screen: one instance, owned by a provider at the root layout for the life of
  the app, and the sermon screen becomes a view of it rather than its owner. While audio
  is loaded, a docked bar sits directly above the tab bar on every tab screen (and above
  the safe area on screens without one): artwork, title, speaker, play/pause, and a
  dismiss that stops and unloads. Tapping the bar opens the sermon screen it came from.
  Opening a message with a YouTube video stops the audio first (`08`: video and audio
  never play together, and background YouTube is a Premium feature we must not imitate).
  Sign-out and account deletion stop and unload it with the rest of a member's state.
  Position keeps ONE owner: the provider writes the throttled local and server position
  exactly as the hook does now, and the mini bar reads it; the sermon screen never
  writes it a second way (the one-fact-one-owner rule in CLAUDE.md, learnt at W2.4).
- **The lock screen has exactly one owner, and the handoff is ours (PR #252, merged to
  `main` as `99da756` on 2026-09-06 while slice 1 was being built).** expo-audio builds its
  lock-screen media3 session with no id, media3 keeps a process-wide registry keyed by id,
  and `AudioControlsService.setActivePlayerInternal` releases the old session synchronously
  but builds the new one inside a coroutine, so two activations that land before the first
  coroutine runs both build with id "" and the second throws `IllegalStateException` on
  Android's main thread, past every JS try/catch, and kills the app (Sentry AGBC-MOBILE-3,
  the tablet, 2026-09-05, against the build in Play review). `features/watch/lockScreen.ts`
  now owns the handoff: **never call `player.setActiveForLockScreen` directly; go through
  `activateLockScreen()` and `releaseLockScreen()`.** Two players are fine; two activations
  without a stand-down between them are what crash. Upgrading expo-audio does not help
  (57.0.4 is iOS-only; the Kotlin is byte-identical). The crash was never reproduced on
  purpose (four cold-start handoffs at 16, 3, 2 and 1 s never collided; the window is one
  coroutine hop wide), so the invariant is proven by five mutation-checked unit tests and the
  device run proves nothing regressed. **Slices 2 and 3 start by merging `main` into this
  branch**, because both touch `useSermonAudio.ts`, which #252 changed.
- **Sequencing agreed before this starts:** (1) the boundary fix PR, (2) the W4.8 docs PR
  (the stashed runbook and `18` edits plus tonight's findings, and this file), (3) the
  in-flight `openExternal` refactor committed by the session that owns it, (4) `main`
  pulled clean, then `feat/w4-9-audio-shelf-and-player` off it.

## 3. Slices

### Slice 1 · The shelf tells the truth and accepts only what plays (dashboard)

- [x] **The top banner changes subject.** The red "X has no audio yet" `Notice`
      (`Shelf.tsx`, `loadNewestMissing`) goes. In its place, at the top of the shelf, the
      speech-quality guide (`copy.sermonAudio.guideTitle` / `guideBody`), promoted from
      its mid-page slot and extended with the format rule: **MP3 only; if the export is
      m4a, aac or wav, save it as MP3 first**, in plain words (no command line, decided at
      frame approval; the recipe lives in the runbook for whoever helps). The "newest message without
      audio" fact can stay as a quiet line on that message's row, which already carries
      "NO AUDIO" and "Add audio"; the shelf's counters already show how many are missing.
- [x] **MP3 only, at every layer that decides.** `ACCEPT` in `AudioUploader.tsx` becomes
      `.mp3,audio/mpeg`; `AUDIO_EXTENSIONS` in `server/sermonAudio.ts` becomes `['mp3']`
      (the mint refuses anything else, so a renamed picker cannot slip a file through);
      `isAudioMagic` drops the `ftyp` branch so an m4a renamed `.mp3` is refused by its own
      bytes; the bucket's `allowed_mime_types` is narrowed to `audio/mpeg` by a migration
      (defence in depth, and the only layer a service-role writer meets). Copy: `dropSub`,
      `pickNotAudio`, and the shelf guide all say MP3.
- [x] **The cap is 50 MB and the refusal says so.** `MAX_AUDIO_BYTES` = 52 428 800 with a
      comment naming the Free-plan limit; `dropSub`, `pickTooBig` and `guideBody` say
      50 MB. `uploadViaXhr` maps a 413 to the size text and any other 4xx to a "storage
      refused this file" text, and only a network error to "check your connection". The
      bucket's `file_size_limit` comes down to match in the same migration.
- [x] **Edit the facts of an audio-only message.** On `SERMON-AUDIO-MANAGE`, when
      `youtubeId === null`, a form with title, speaker, series, date preached, prefilled,
      one Save. Server: `updateAudioOnlySermon()` in `server/sermonAudio.ts` performs a
      single UPDATE whose WHERE carries `youtube_id is null`, so a synced row cannot be
      edited even by a crafted request; validation reuses `validateCreate`'s rules
      (title 1-200, date `YYYY-MM-DD`, speaker required). Action `editAudioOnlyAction`
      in `actions.ts` behind `authorize()`, redirecting to the manage screen with an
      outcome line (`OUTCOMES` gains `edited`). No audit row: content edits are not
      privileged actions under ADR 0015 (roles, moderation), and `17` §4 already treats
      the shelf as ordinary admin work. Grants: `sermons` already grants UPDATE to
      `authenticated` under "admins manage sermons", so no migration for the write.
- [x] **Frames.** `dashboard.html`'s `SERMON-AUDIO-MANAGE` gains the edit block for the
      audio-only variant and loses nothing else; the shelf frame's top banner is redrawn.
      **Approved by Ayo 2026-09-06**, with one change: no command line in the note (the
      uploader is not technical), so the guide says "save it as MP3 first, from your editing
      app or a free converter" and nothing more.
- [x] **Verification.** Vitest `server` project: the mint refuses `m4a`; the byte check
      refuses an `ftyp` file; `updateAudioOnlySermon` edits an audio-only row and leaves a
      synced row untouched (assert the row after, not the return value). Vitest `ui`:
      the picker's `accept`, the 50 MB refusal text, the edit form's prefill and its
      outcome line. pgTAP: the bucket's MIME list and size limit. Then the real thing,
      under `next start` with a minted session (the throwaway-test recipe in the
      dashboard verification memory): upload an m4a and read the refusal; upload an MP3
      over 50 MB and read the refusal; edit a title and see it in the app.
      **Built 2026-09-06** (`20260906120000`, pgTAP 034 at 23 assertions, 15 server tests,
      346 dashboard tests). Two things the build found: the byte check had accepted ADTS
      AAC because it shares MP3's frame sync (the Layer III bits now tell them apart), and
      `readDurationViaAudio` waits for `loadedmetadata` with no timeout, so a tab Chrome
      considers hidden sits on "Reading the file…" until it is shown again (seen driving
      the form from an unfocused tab; it resumes on focus, so a delay rather than a hang,
      and left as is). Browser verification under `next start`: shelf, attach copy, the
      m4a refusal, the synced manage page without the facts form, all as the frames; then an
      audio-only message created through the real form with a clip cut from the production
      MP3, its facts corrected through the new form, and the row read back from the database
      with the new title, series and date and the same audio_path.

### Slice 2 · Seek and speed (app, player)

- [x] **Frame first.** `.pl-scrub .bar` in `entry-flow.html` (both W3.1 audio-mode
      frames, light and dark) gains a knob at the played edge: 14 px, gold, the same
      radius token. `05` is synced in the same change. **Approved by Ayo 2026-09-06.**
- [x] **Seek.** `AudioTransport.tsx` stops being purely presentational: the bar takes a
      `PanResponder` (grant, move, release) and a tap; both map x to a fraction of the
      laid-out width (`onLayout`) and call `onSeek(fraction * durationSec)`. While a drag
      is live the bar and the clock follow the finger, not the player, and the seek is
      issued once on release, so the player is not asked for a hundred seeks a second.
      `useSermonAudio` exposes `seekTo(sec)` (it already calls `player.seekTo` for resume
      and the 15 s buttons, line 211); the throttled position write follows a seek exactly
      as it follows a 15 s tap. **Nothing lock-screen related follows it**: media3 reads the
      position from the player itself, and this slice never touches `setActiveForLockScreen`
      or `lockScreen.ts` (an earlier draft of this line assumed a "lock-screen metadata
      update" that the app has never called; see §5). The touch target is
      the full bar height plus the `hitSlop` that brings it to 44 px (CLAUDE.md), without
      changing the drawn 5 px.
- [x] **Accessibility.** The bar gets `accessibilityRole="adjustable"`, a label
      (`watch:scrubLabel`), a value reading the clock, and `accessibilityActions`
      increment / decrement mapped to the existing 15 s steps, so a screen-reader user
      seeks by the same control sighted users drag. Copy keys in all four languages.
- [x] **Speed.** `SPEEDS` in `features/watch/audio.ts` becomes `[1, 1.25, 1.5, 2]`; the
      cycle button is unchanged. The stored-speed fallback in `nextSpeed` already lands an
      unknown value at 1x. The 2x ceiling and its reason (the Android clamp) are recorded
      in a comment beside the constant.
- [ ] **Verification.** `apps/mobile/src/test/expoAudio.ts` gains `seekTo` recording;
      tests assert a drag to 30% of a 96-minute message issues one `seekTo(1728)` on
      release, that a tap seeks, that the clock follows the finger during a drag, that
      the a11y actions seek by 15 s, and that the speed cycle visits 2 and returns to 1.
      Then the device: seek while playing and while paused, seek past the end (the
      30-second-from-end rule in `08` still applies), lock screen shows the new position,
      both themes, maximum font size with the longest title, and one run with TalkBack.
      **Built and verified 2026-09-06** (53 player tests, 1021 across the mobile suite,
      tsc, eslint). On the tablet (`SM-X826B`, dev client against the local stack): a tap
      at a quarter of the bar landed on 3:00 of 12:00 and a drag to three quarters on
      9:00, paused; a drag to the middle while playing put the media session at
      `position=360085` (6:00), which is what the lock screen reads; a drag off the right
      edge clamped to 12:00; the speed tile cycled 1.25x, 1.5x, 2x, 1x; the knob and clock
      held in light and dark and at font scale 2.0; `logcat -b crash` stayed empty of
      "Session ID must be unique" throughout, and `dumpsys media_session` showed one
      anonymous session line for the app. **Not done, honestly:** the native rate was not
      read back from the session dump (the drive was flaky; the unit test proves the
      `setPlaybackRate(2)` call and expo-audio's own clamp keeps 2.0), a phone width (the
      S22 was not attached; the knob's position is width-relative), and TalkBack itself
      (the accessibility node reads "Playback position, 4:53 -7:07" in the dump, which
      proves the role and value; the increment and decrement actions are unit-tested).
      One thing seen and not fixed: a font-scale change restarts the dev client onto Home,
      an artefact of driving it, not of the app.
      **Then Ayo found the defect the matrix had not: a drag whose finger drifted off the
      bar snapped back.** Android's native scroll view intercepts a vertical move within
      the first few events, whatever the JS responder's termination request says, and a
      `scrollEnabled={false}` set from the grant reaches the native side a frame too late
      (two drifting drags on the tablet landed nowhere near the finger). So the bar became
      a `react-native-gesture-handler` pan (already a dependency through Expo Router, so
      no native rebuild), claimed natively on 2 points of sideways movement, with
      `GestureHandlerRootView` at the root layout and the library's setup file in Jest.
      What a finger leaving the bar means is one pure function, `shouldSeekAfterTouch`: a
      tap, a finished drag and a drag the system interrupts all seek to where the finger
      last was (what Android's own seek bar does on cancel), and a vertical swipe that
      happened to start on the bar seeks nothing. The screen also stops scrolling from
      touch-down, belt to the gesture's braces. Proven on the tablet: a 25% to 75% drag
      climbing 300 px landed on 9:00, a 75% to 40% drag dropping 300 px on 4:48, a tap on
      6:00, a vertical swipe on the bar left it at 6:00; 55 player tests, 1023 across the
      suite. The plan's earlier claim that the responder props were enough was wrong for
      exactly one reason, and it is written beside the gesture.

### Slice 3 · Keep listening while browsing (app, mini player)

- [x] **Frames first, three of them.** `NOW-PLAYING-BAR` docked above the tab bar, drawn
      on the Home frame (light and dark): 56 px, artwork square at the left, title over
      speaker in the middle, play/pause and dismiss at the right, a 2 px gold progress
      line along its top edge. One frame of it over a screen WITHOUT a tab bar (the
      testimony detail, say) to fix where it sits there. The tablet two-pane `WATCH`
      frame shows it across the rail. Ayo approves all three; `05` gains the pattern. **Composed and approved 2026-09-06** (in the W3.1 audio gallery, cloned from HOME checked-in and MY-LIST, and on the tablet WATCH frame; `05` carries the pattern).
- [x] **The player moves up.** A `NowPlayingProvider` in `app/_layout.tsx` owns one
      expo-audio player for the app's life (`createAudioPlayer`, replaced only when a
      different message is loaded, released on sign-out, deletion and dismiss).
      `useSermonAudio` becomes `useNowPlaying()`: the same surface the sermon screen and
      `AudioMode` consume today, backed by the provider instead of a screen-scoped hook.
      The blur pause goes; the AppState background handling, the lock-screen activation
      (through `activateLockScreen`, never the player directly), the resume-on-open rule
      (newer of local and server, `08`) and the throttled position writes all move with
      the player unchanged. The release guard from W3.1 slice 3 (the try/catch with the
      reason beside it) stays, now on the provider's own unmount and on dismiss.
- [x] **The handoff becomes the primary path, so it is the provider's, not a hook's.**
      "Loading a second audio message replaces the first" IS the two-player handoff that
      killed the app on 2026-09-05, promoted from an edge case to the main road. The
      release effect that `useSermonAudio` gained in #252, keyed on `[player]` ALONE, moves
      to the provider: when the provider swaps players, the outgoing one is stood down
      (`releaseLockScreen`) before the incoming one activates, and the same stand-down
      runs on unmount, dismiss, sign-out and deletion, beside the object-release guard.
      Keep it keyed on the player alone: folding it into an effect that also depends on
      title, artist or artwork tears the session down and rebuilds it on every metadata
      change, which reopens the exact race.
- [x] **The lock screen follows a metadata change, in place.** Decided with Ayo on
      2026-09-06 (folded in here rather than its own item). Found by #252: the app has
      never called `player.updateLockScreenMetadata` (zero call sites), and the activation
      effect runs at most once per player, so a picture or title changed on a message
      someone is listening to stays stale on the lock screen until the player is rebuilt.
      The provider calls `updateLockScreenMetadata(metadata)` when the loaded message's
      title, speaker or artwork changes; expo-audio routes it to `updateMetadataInternal`,
      which updates metadata and artwork WITHOUT rebuilding the session. Re-activating to
      refresh is exactly what must never happen. Test: change the artwork of the loaded
      message in the fake, assert one `updateLockScreenMetadata` call and zero
      `setActiveForLockScreen` calls.
- [x] **The bar.** `features/watch/NowPlayingBar.tsx`, rendered by `AppTabBar`'s host so
      it sits above the bar without each tab screen knowing about it, and by the root
      layout on screens without a tab bar. Hidden when nothing is loaded. Tap opens
      `/sermon/[id]` for the loaded message; the sermon screen, finding the provider
      already holds that message, shows it without reloading or seeking. Play/pause is
      the provider's; dismiss stops, writes the final position, unloads. Reduced-motion
      respected on any progress animation. Touch targets 44 px.
- [x] **Two rules enforced in code, not copy.** Opening a video message calls the
      provider's `stop()` before the YouTube embed mounts. Loading a second audio
      message replaces the first and writes the first's final position before it goes.
- [x] **Verification.** Unit, against the expo-audio fake: navigating away does not
      pause; the bar appears when audio is loaded and disappears on dismiss; tapping
      the bar routes to the right id; opening a video stops audio; sign-out unloads;
      exactly one position write path exists (assert the sermon screen has no writer).
      Device: play, tab to Home, Family, Give and More with the bar present on each;
      open a testimony (no tab bar) and see where the bar sits; lock the phone with the
      bar showing; rotate the tablet; maximum font with the longest title (the bar
      truncates, never grows); TalkBack reads the bar as one control with two actions.
      Then the W3.1 background test again, because the player's owner changed: 15
      minutes locked, controls working, interruption survived, resume where it stopped. A
      wrongly-timed release kills background playback after about three minutes, and that
      is the one regression this slice could plausibly cause, so it is not skippable.
      **The lock-screen instrument** (from #252): `adb shell dumpsys media_session | grep
      "Media button session"` must show ONE line for `com.oami.agbcapp`, with an anonymous
      id (`...androidx.media3.session.id./254`); the trailing number changing across a
      handoff is the proof the handoff happened. And `adb logcat -d -b crash | grep
      "Session ID must be unique"` stays empty. Two messages with audio exist locally for
      the handoff (`b367a753-8d32-45a7-a79b-3180820c010e`,
      `95fa49a2-b000-49c2-9267-5786b41ab6f5`; `agbcglobal://sermon/<id>`). If a reliable
      reproduction of the collision turns up while building this, capture it in a test and
      tell Ayo: the fix was never watched stopping the crash.
      **Built and verified 2026-09-06.** `features/watch/nowPlaying.tsx` is the provider,
      `NowPlayingBar.tsx` the bar, `useSermonAudio.ts` is gone and `AudioMode` consumes the
      provider; the screen keeps only the video embed's own position writer. Unit: 38 in
      `sermonAudio.test.tsx` (leaving keeps playing, the app closing writes and releases,
      coming back shows the playing message without reloading, an unfocused screen keeps
      its hands off, a video screen stops it, sign-out unloads, a fact change goes through
      `updateLockScreenMetadata` with no second activation, the re-mint stands the old
      player down BEFORE removing it and activates the fresh one after, a refused mint
      shows the retry) and 6 in `nowPlayingBar.test.tsx` (nothing loaded draws nothing,
      tap routes to the id, the disc plays and pauses, play after the end seeks to 0
      first, the cross releases and the bar goes, hidden on the player's own route and
      only there); the fake now mints a DISTINCT object per `createAudioPlayer`, because
      the provider keys its handoff on the player's identity and a fake that handed the
      same object back hid a missing re-activation; 1036 across the mobile suite, tsc,
      eslint. Every new assertion was watched failing against the code it guards. Device, the tablet against the local stack: the
      bar stood on Home, Family, Give, More and Settings (a pushed route, so the
      bottom-edge host), in portrait and at font scale 2.0 (same 56 px, title on one line,
      truncated), and with the display overridden to a phone (`wm size 1080x2340`,
      `density 420`: above the tab bar on Home, at the bottom edge on Settings); a tap on
      it reopened the player on the live position (11:43, still listening, no second
      signed-URL mint in the storage log); the handoff to the second message moved the
      media session from `./289` to `./291` with ONE app session in the stack and an empty
      crash buffer; the Video segment emptied the session and the bar; the cross emptied
      the session; play on a finished message restarted it (`position=14`). **The background test:** locked at 2:43 of the
      12-minute test file, playing at 2x, the tablet dozed and the message ran to its
      end (`position=720040`, STOPPED, at 15 minutes), so playback survived 4.6 minutes
      of wall time past the lock, beyond the three-minute mark the plan warns about, with
      the crash buffer empty; the media play key on the finished session started it over
      (`position=14`, PLAYING). A second pass to see the controls: locked near the start,
      still PLAYING after four minutes dozing, the lock screen woke to the media panel
      (artwork, title, seek back and forward, pause), the pause key paused it, the play
      key resumed it (`position=674515`), and the app came back to it playing.
      **Three defects the suite could not see, all found on the tablet and each now
      pinned by a test that fails without its fix.** (1) Every exit from the player killed
      the audio: `TabletShell` remounts the whole stack when the layout changes, so the
      sermon screen mounted again with fresh state on the way out, in video mode, and its
      own rule stopped the player; the mode now seeds from the provider (the same seed is
      what makes the bar's tap land in audio mode). (2) With that fixed, the SECOND
      message's audio still died on the way to Home: the remount renders every sermon
      screen in the history at once, and a message visited earlier, in video mode, stopped
      the one playing now; the rule became a focus effect, and the test's `useFocusEffect`
      mock gained a focus flag so an unfocused mount can be modelled. (3) `play()` on a
      finished native player stays put, so the bar's disc did nothing after a message
      ended; `toggle` seeks to 0 first when the position sits on the duration. Also
      fixed: a synced message carries its speaker in the title and nothing in `speaker`,
      so the label read "…, " with a dangling comma and the bar drew a blank second line.
      **Seen and not fixed, recorded for W4.7's shell:** `uiautomator`'s accessibility
      tree goes stale on the tablet after navigation (it kept reporting the transport of a
      screen already left, and none of the bar's nodes) until a rotation refreshed it;
      touches were unaffected (a tap on a phantom node reached the visible screen, and
      the audio played on), so whether TalkBack sees the same staleness is unproven, and
      TalkBack itself was not run; the bar is three buttons (open, play/pause, stop) with
      their labels verified in the dump, not the "one control with two actions" written
      above. Also seen: at font scale 2.0 the tablet player's disc sits below the fold
      (pre-existing), and a display override restarts the dev client (an artefact of the
      drive). The metadata refresh is unit-proven only: no fact changed under a listener
      on the device.

### Slice 4 · Audio has a home in Watch (app)

- [x] **Frames first, four of them.** `WATCH` gains a `.seg` control under the title
      (Video · Audio), the pattern the Family frame already uses at line 2280. New
      frames: `WATCH · Audio` (the hero is the newest audio-only message, then one list
      newest first with "See all"), `WATCH · Audio · empty` ("Nothing to listen to yet"
      with the grace-framed line and no dead button), and the tablet two-pane variant
      (`WATCH · tablet landscape` exists and must show the segment too). A surface is a
      state (CLAUDE.md), so the empty variant is not optional. Ayo approves all four.
      **Composed and approved 2026-09-06** (the phone WATCH frame renamed `Watch · guest ·
      Video segment`, two new frames beside it, the tablet list pane's never-built Latest /
      Series / Live control replaced by the same segment; hero and rows of an audio-only
      message wear a waveform disc, `.mediahero .wavec` and `.rthumb .wave`, not the play
      glyph; no "via YouTube" line under the Audio segment).
- [x] **Data.** `SermonSummary` already carries `youtube_id` and `audio_path`, so the
      Audio segment is a filter on the query Watch already holds
      (`youtube_id === null && audio_path !== null`), newest first. No new query, no new
      backend. "See all" opens the existing list route with an `audio` filter parameter;
      the route stays routable with or without it (`04`: no dead ends).
      **Built 2026-09-06.** `features/watch/segment.ts` holds the rule (`isAudioOnly`:
      `youtube_id` null and `audio_path` set), the three-way split of the one feed, and
      the session store. One thing the split corrects on the way: an audio-only row
      carries the sync's default `video` kind, so before the segment it LEAKED into the
      Video rail and, when newest, took the hero; the Video half is now everything that
      is not audio-only. `watch-search` reads `list=audio` (and `list=videos` now leaves
      the audio-only rows to it), with its own "All audio messages" header, the
      grace-framed empty state and no channel link. The tablet's `WatchListPane` draws
      the same segment from the same store.
- [x] **Segment state.** The chosen segment persists for the session in the Watch
      screen's Zustand slice (not the URL), and Home's "Watch" button lands on Video as
      today. Deep links from notifications are unaffected. **Built:** `useWatchSegmentStore`
      (zustand, not persisted); Home's three "Watch" buttons call `landOnVideo()` first.
- [x] **Search.** No change in shape. Confirm that results for audio-only messages draw
      the audio cover (`artwork.ts` precedence) and carry the duration; add the missing
      i18n key if a row needs "Audio" as its kind label. **Built:** no kind label was
      needed, because the row itself says it: `SermonRow` wears the LISTEN glyph (the
      waveform disc, `.rthumb .wave`) for an audio-only message wherever it is drawn,
      search included, and `MediaHero` takes `glyph="listen"` for the Audio hero; the
      duration was already on the row.
- [x] **Verification.** Unit: the filter, the empty state, the persisted segment. Device:
      the matrix in `21` §4 for both segments (small phone width via `wm density`, the
      tablet `SM-X826B` for the two-pane frame, maximum font with the longest title, both
      themes, offline). Maestro: extend the Watch journey with one Audio-segment step.
      **Verified 2026-09-06.** Unit: `segment.test.ts` (the rule, the split keeping the
      feed's order, the store, `landOnVideo`) and eight new cases in `watch.test.tsx`
      (Video is the default and hides the audio-only message that used to take its hero;
      the Audio half's hero, list, listen glyphs, See all to `list=audio` and no YouTube
      line; the empty state with no button; the chosen half surviving a remount; the audio
      list and the videos list splitting the same rows; an audio-only search result's
      glyph and duration); eight of them fail with `isAudioOnly` mutated to `false`. 1050
      across the mobile suite, tsc, eslint, prettier. Device, the tablet against the local
      stack with two audio-only rows inserted by hand (the sync never makes one): the
      segment under the title, the Audio hero with the waveform disc, the second message
      listed with its glyph, See all opening "ALL AUDIO MESSAGES" with both rows and no
      channel link, tapping the hero opening the player in audio mode with the two-pane
      list showing the same segment and row; Audio still chosen after Home and back;
      Home's "See all" on the latest message landing on Video; the display forced to a
      phone (`wm size 1080x2340`, density 420) for both halves; font scale 2.0 (segment
      labels capped, the empty state's copy scaling); both themes; offline (PostgREST
      stopped) painting the cached half with no error. The Maestro guest smoke ran from a
      cleared state with the two rows set unavailable and its new step saw "Nothing to
      listen to yet". **Seen and worth knowing:** the persisted feed is served for five
      minutes without a refetch, so a message made available in that window shows on the
      next pull or launch after it, not at once (the query's own `staleTime`, unchanged);
      and the See all list opens with the search keyboard up because the search field
      autofocuses without a term (pre-existing, the same for the videos list). Not run: a
      physical phone (no phone attached; the phone layout was the display override).

### Slice 5 · Docs land, plan leaves

- [ ] `08`: the seek control, the speed ceiling and its reason, MP3-only and the 50 MB
      cap, and a new clause: listening survives navigation, the mini player, and the
      video-stops-audio rule. `05`: the knob, the Watch segment and the now-playing bar. `17` §4: the edit form and the format
      rule. `02` §Storage: the narrowed bucket. `18`: the audio-owner line gains "and the
      file is MP3 under 50 MB". `25`: the W4.9 entry with the lessons. CLAUDE.md's
      orientation paragraph. Then delete this file.

## 4. Order, gates, and what each slice must not do

- BE-first inside each slice (`25` §1): the mint, the byte check and the update routine
  are proven in Vitest and pgTAP before a form exists for them.
- Frames before screens, approved by Ayo, read first-hand from the mockup in the build
  session, never from this file's description of them (the three-strikes rule in the
  build-from-reference memory).
- A slice is done only after the device matrix, not after green tests: this item exists
  because a green suite never rendered the one screen that mattered.
- Slice 1 must not touch the sync job or synced rows. Slice 2 must not add a native
  dependency. Slice 3 must not leave the sermon screen with a player of its own, and must
  not add a second writer of the position. Slice 4 must not add a query; the filter is on
  data Watch already holds.
- The dashboard redeploys on merge while the bucket migration waits for the manual
  dispatch (`21` §3), so slice 1's PR ends by dispatching `supabase-deploy.yml` and
  checking production's migration head, or its MIME narrowing exists only in the repo.

## 5. Open questions (answer in the build session, not here)

- Whether "See all" for audio deserves its own route name for deep links, or the filter
  parameter is enough. Decide when composing the frame.
- Whether the shelf should show the newest-without-audio message anywhere once the red
  banner goes, or the counters are enough. Decide on the frame.
- Whether to keep m4a acceptance for a transition week for anyone with files already
  exported. Default is no: the church has one uploader and the recipe is one line.
