# W4.17 · AGBC Global reaches the App Store (release 1.0.2 on both stores)

**Status:** WAITING ON APPLE (2026-09-15). **Android 1.0.2 (24) is LIVE on Google Play**
(released 2026-09-14). **iOS 1.0.2 (24) was SUBMITTED to App Review on 2026-09-15 at 06:38 UTC**
and is "Waiting for Review", with a MANUAL release. Slices 1 to 7 are done. **What is left is
§6, the checklist for the day Apple approves and the week after it**: work it top to bottom and
tick each box in the PR that does it. This file is the resume point for every session in this
item and is deleted by the session that closes it, once `18`, `19`, `releases.md` and
`store-submission.md` carry what a later reader needs.

**Where it came from:** Ayo, 2026-09-14, once the church's Account Holder had added him to the
Apple Developer team as Admin. "no iphone device here yet. we will not need to test. just let
do all the rest, the build, upload, update the ios app name, screenshots, etc and upload the
release." And: "the ios version should match the android version name".

**Refs:** `18` §Launch checklist (the Apple, APNs, staged rollout and iOS E2E lines) · `19`
(frozen identity, listings, age ratings) · `03` §Security (the review bypass) · `15` (APNs,
universal links) · `docs/runbooks/store-submission.md` (§1 bypass, §2 review notes, §3 the Play
upload, §3b iOS) · `docs/runbooks/releases.md` · `docs/store/` · `Desktop/agbc`
`docs/SPEC-app-links.md` · `~/.claude/standards/` `mobile.md` §Releases, `devops.md`,
`qa-testing.md`

---

## 1. Decisions, with the reasons

- **Both stores ship 1.0.2, built from one commit on `main`** (Ayo, 2026-09-14, picked over two
  alternatives). Android 1.0.1 (23) was built from tag `v1.0.1`, and `main` has since gained
  W4.11 (tablet orientation, R8), W4.15 (share as a picture), W4.16's app (the rhythm calendar,
  whose database is already live) and #264 (Expo SDK 57 alignment, React Native 0.86.3). An iOS
  build from the tag would have carried the portrait lock W4.11 removed, onto iPads, and any fix
  the first iOS build needed would have had to land twice. An iOS "1.0.1" from `main` would have
  meant two different apps under one version number, and the release table would have stopped
  being true. So Android gets a release it was already waiting for, and both stores carry the
  same code under the same number.
- **iOS ships WITHOUT verification on an Apple device** (Ayo, 2026-09-14). There is no iPhone
  in the project. What this accepts, said once so nobody rediscovers it: Apple's reviewer is the
  first person to open the iOS app; a crash there costs a rejection, one of the month's 15 iOS
  builds and a few days, not members; and nothing iOS-only has been seen working (background
  audio and the lock screen, push through APNs, universal links, the share sheet with a PNG,
  the iPad layouts, the update notice). `18`'s iOS E2E line stays open rather than being ticked.
- **iPad is supported, and was never a choice.** Grace Portal 1.0.0 (18) supports iPad (the live
  listing names 80 iPad models and carries iPad screenshots), and an update may not drop a device
  family the version before it supported. Expo defaults `ios.supportsTablet` to false, so the
  first upload would have failed without it. It costs 13" iPad screenshots.
- **The minimum iOS version rises from 15.6 to 16.4.** Grace Portal supported 15.6; SDK 57's
  Expo modules declare 16.4 (`ExpoModulesCore.podspec`). An iPhone below 16.4 keeps Grace Portal,
  which the App Store does on its own. No decision to make, but it belongs in the record.
- **W4.10's iOS update notice is switched on** with `ios.infoPlist.AppStoreID`. The reasoning,
  including why it cannot fire before AGBC Global is on the store, is on the key in
  `app.config.js`.
- **The store links in the app and on the website wait until AGBC Global is LIVE on iOS.**
  `APP_STORE_URL` in `apps/mobile/src/lib/links.ts` and `appStoreUrl` in `Desktop/agbc`
  `src/lib/site.ts`. Filled today they would send an iPhone to Grace Portal, whose sign-in is
  retired. The association file does not wait (§3, slice 5).

## 2. What the consoles hold, read 2026-09-14 (not typed from memory)

| Fact | Value | Where it was read |
|---|---|---|
| Team | Amazing Grace Bible Church Global LTD, Team ID `58MF8H28K5`, Organization | developer.apple.com, Membership details |
| Roles | Account Holder Olayinka Ademiluka; Ayo is Admin | same |
| Membership | Fee waived, renews 2027-03-10; both agreements accepted 2026-09-11 | same |
| App ID | `com.olayinkaademiluka.grace-portal` exists (description still Grace Portal's) | Identifiers |
| Certificates | Two Distribution (one Xcode-managed, to 2027-03-14; one to 2027-05-17) and one Development, all created by the Account Holder | Certificates |
| Keys | **None.** No APNs key has ever existed on the team | Keys |
| App record | Grace Portal, **Apple ID `6760579106`**, SKU `AGBC-GracePortal`, primary language English (U.K.) | App Store Connect, App Information |
| Live iOS version | 1.0.0 (18), released 2026-05-20, iOS 15.6+, Lifestyle, rated 4+, English only; the store reports the version as the literal string `1.0.0 (18)` | App Store Connect and the iTunes lookup API |
| Builds uploaded | **17 and 18 only.** `19` and `credentials.md` said 19, which was Android's | TestFlight |
| Android | Production track Active, latest release **23 (1.0.1)**, updated 2026-09-08 | Play Console |
| Pending notice | "Review New Social Media Questions on Age Ratings" | App Store Connect, Apps |

## 3. The slices, in order

Every commit, push, tag, submission and secret change waits for Ayo's explicit go, one at a time.

- [x] **Slice 1 · Config and corrections (one PR).** `version` 1.0.2 (and `package.json`);
      `ios.supportsTablet`, `ios.config.usesNonExemptEncryption`, `ios.infoPlist.AppStoreID`,
      each with a test in `__tests__/app-config.test.ts` that was mutation-checked; the AASA
      comment corrected (the Team ID is on the Membership page, not only in App Store Connect).
      Docs: `19` and `credentials.md` (the last iOS build was 18; the Apple row), `18` (the
      no-device decision on the Apple and iOS E2E lines), `store-submission.md` §3b, `25`'s board
      entry, and this file. Verified: typecheck, lint, `i18n:check`, `store:check`, the mobile
      suite, and `expo config --type introspect` showing the Info.plist and entitlements.
      **DONE: PR #313, merged 2026-09-14**, with `ascAppId` for `eas submit` added to `eas.json`
      as its second commit.
- [x] **Slice 2 · Signing (Ayo, in PowerShell, one step at a time).** `eas credentials -p ios`
      signs in to Apple (two-factor, so interactive). EAS cannot reuse the existing distribution
      certificates without their private keys, so it makes its own; that is safe on iOS, where
      the identity that must never change is the bundle id and the team, not the certificate.
      Same session: the **APNs key** (the team has none) and an **App Store Connect API key** for
      `eas submit`. Then `eas build:version:set -p ios` to 23, so the production profile's
      auto-increment makes iOS build 24 alongside Android's 24. Check each command against
      Expo's current docs at the time, never from this file.
      **DONE 2026-09-14**, with eas-cli upgraded 21.2.0 to 24.3.0 first. Now on EAS: a
      distribution certificate (serial `1776D0B2...`) and provisioning profile (`9Q7G6CX9V5`),
      both to 2027-09-14; a push key; an App Store Connect API key with the **App Manager** role
      (the least that can upload, chosen over Admin because EAS stores it); iOS build number 23.
      **Two traps, both on the record here because the next `eas credentials` session hits them:**
      run it from `apps/mobile`, never the repo root, where EAS finds the stub `app.json`
      `.gitignore` documents and offers to "configure this project"; and **Apple sign-in fails
      with "iTunes service key is empty"** (expo/eas-cli#4392, open since 2026-09-11) because
      Apple's `olympus/v1/app/config` endpoint now answers 404. The workaround is
      `EXPO_APP_STORE_AUTH_SERVICE_KEY=e0b80c3bf78523bfe80974d320935bfa30add02e1bff88ec2166c6bd5a706c42`,
      which `@expo/apple-utils` reads before it scrapes. That value is not a secret: it was
      matched against Apple's own App Store Connect login code (`ASC.BRGDgul9.js` on
      `unpkg.apple.com`, `getIDMSAppIdKey`) rather than trusted from the forum post that
      suggested it, and sign-in still goes only to `idmsa.apple.com`.
- [x] **Slice 3 · Tag and build.** Tag `v1.0.2` on the merged commit (`releases.md` step 2),
      then `eas build --platform android --profile production` and `eas build --platform ios
      --profile production` from it. Record build ids, commit and fingerprint.
      **DONE 2026-09-14.** Tag `v1.0.2` on `c66e6c6`. Android `23b3222f`, 1.0.2 (24), fingerprint
      `8d6f9d40`, 19:35 to 20:32 UTC (33 minutes of it in the free-tier queue). iOS `24f10277`,
      1.0.2 (24), fingerprint `9db25415`, 20:17 to 20:26 UTC; its signing passing is the proof the
      new profile carries Associated Domains and push. `eas submit` (`84d5091d`) uploaded it and
      TestFlight shows it "Ready to Submit" with no export compliance question, so
      `usesNonExemptEncryption` did its job. EAS also created an internal TestFlight group,
      "Team (Expo)", which is its default and harmless.
- [x] **Slice 4 · Android 1.0.2 walked by hand, then uploaded.** Convert the AAB and install it
      on the S22 (the route W4.8 used for build 21). The walk is owed by four earlier items and
      no suite can stand in for any of it: W4.11's **R8** (every screen that loads a native
      module: Watch video and audio, the player and lock screen, sign-in, testimony photo pick,
      share as a picture, notifications, the map), `18`'s **tray icon** on a real push, W4.16's
      **rhythm** ring and Next card, and W4.15's **share cards**. Upload by hand, release notes
      per locale, verify from Publishing overview (`releases.md` step 5). **After it is live:**
      a Play-installed 1.0.1 device should get W4.10's update sheet, the first time that feature
      can be seen at all.
      **DONE DIFFERENTLY, BY DECISION (Ayo, 2026-09-14): no hand walk and no internal testing
      track; straight to production** ("we upload directly to prod since we already tested
      locally"). The risk that accepts, said once: R8 only runs in a release build, so neither
      local testing nor a dev client exercised it, and a stripped class would surface on members'
      phones, recovered by halting and shipping 1.0.3. **Live 2026-09-14**, verified three ways:
      Publishing overview (nothing pending), the production track ("Active · Latest release 24
      (1.0.2) · 177 countries/regions · 7 installs") and the public store page ("Updated on 14
      Sept 2026"). Release notes in en-US (Play's default language here, not en-GB), de-DE, nl-NL
      and fr-FR, written for 1.0.2 rather than the Grace Portal transition copy. Play's preview
      read: no devices lost (tablets +6, TV +3, cars +17), **22 MB for new installs, 11.1 MB
      smaller than 1.0.1**, required features 3 to 2 (the portrait requirement gone with W4.11's
      lock), and no deobfuscation warning, which every earlier release carried. **Partial R8
      evidence after the fact**: slice 6's captures drove Home, Watch (both halves), the map,
      branches, the language switcher and the share preview (card, QR and all) on the
      Play-installed 1.0.2 on both devices without a fault. Still unseen: audio and the lock
      screen, sign-in on a fresh install beyond Ayo's own, photo pick, the tray icon on a real
      push. Play's release dashboard now lists three recommendations on 24 (edge-to-edge,
      already known and not ours; bitmap optimisation; R8 optimisation), none acted on.
- [x] **Slice 5 · The association file (website PR in `Desktop/agbc`).**
      `public/.well-known/apple-app-site-association` with `58MF8H28K5.com.olayinkaademiluka.grace-portal`,
      per `docs/SPEC-app-links.md`, verified with its `curl` step. Safe before the app is live:
      Grace Portal claims none of these links.
      **DONE: `agbc-website` PR #51, merged and live 2026-09-14.** Verified on the Vercel preview
      first (200, `application/json`, no redirect, the right body; read through a signed-in tab,
      because previews sit behind Vercel's login) and then on `www.agbcglobal.com`. Apple's CDN
      copy still answered 404 that night, which it does until Apple refreshes. "Grace Portal
      claims none" is checked, not assumed: its Xcode project in `Desktop/agbc-app` has no
      entitlements file.
- [x] **Slice 6 · App Store copy and screenshots (one PR).** **Most of the copy already exists**:
      W4.8 wrote the Apple-only fields into `docs/store/listing.json` for all four locales
      (`subtitle`, `keywords`, `promotionalText`, with Apple's limits under `limits.apple`,
      which `store:check` measures), and `docs/store/data-safety.md` carries an "Apple: privacy
      nutrition labels" section and the Apple age rating reasoning. What this slice owes is
      reading those against the app as 1.0.2 ships it (W4.15 and W4.16 postdate them), release
      notes for 1.0.2 that also work as the Grace Portal transition copy on iOS, and the age
      rating's NEW social media questions, which postdate `data-safety.md`; the answers stay
      consistent with Play's and `19` (moderated user content, accounts 16+), so not 4+. Screenshots:
      `scripts/capture-store-screenshots.sh` extended to Apple's iPhone 6.9" and iPad 13" pixel
      sizes (check the accepted sizes in App Store Connect's own help at the time), captured on
      the S22 and the tablet from the installed 1.0.2 build with the system bars hidden, and
      reviewed for anything that could only be Android before they go up.
      **SCREENSHOTS CAPTURED 2026-09-15** (40 files, gitignored under `docs/store/screenshots/`
      `iphone-6.9/` and `ipad-13/`), from the Play-installed 1.0.2 on both devices, signed in as
      Ayo (his decision), dark theme. `STORE=apple` in the capture script does the frame: exact
      size, bars measured and cropped, alpha removed, text at 1.0, portrait locked, everything
      restored on exit. **The App Store set is not the Play set**: Family and Rhythm were empty
      states on production (no testimonies yet, no rhythm for a new member), so the five are
      Home, Watch on Video, share as a picture, the map and branches; the reasons are on
      `capture_apple_set`. No other member's name or post appears (production has none yet).
      Both dev clients were uninstalled for this, with Ayo's OK, so the S22 and the tablet now
      carry the store build and **need a new EAS development build** before the next Metro
      session (#264 changed native code anyway). **Two things for Ayo at upload**: Home's empty
      family card reads "Testimonies are coming", which App Review may read as "coming soon"; and
      **the iPad share-preview screenshots show a real bug** (§5), so the iPad set goes up
      without them unless the fix lands first. **DONE**: the script is PR #314; the copy and the
      age rating were settled inside slice 7 (below), and the one copy change, "across four
      nations" to "across the nations" (Ayo: the church will grow past four), is in
      `listing.json` for all four languages.
- [x] **Slice 7 · App Store Connect, driven in Chrome, every value shown to Ayo before it is
      saved.** `eas submit -p ios` uploads build 24; a new version 1.0.2; name **AGBC Global**;
      the slice 6 copy, screenshots, privacy answers and age rating; review information with the
      notes from `store-submission.md` §2. **The review sign-in has to work for Apple's reviewer**:
      check the bypass's real state on production first (§4), open a window with a fresh code
      per `store-submission.md` §1, and prove it before submitting. Release: manual after
      approval, and all at once rather than phased, for the same arithmetic as Android's
      (`releases.md`), unless Ayo decides otherwise. Submit only on Ayo's explicit go.
      **DONE: SUBMITTED 2026-09-15 06:38 UTC, "Waiting for Review"**, on Ayo's go, after he
      put the review address and the current code into the sign-in fields and proved them in
      the app on the S22. Every value below was read back from App Store Connect's own API
      (`/iris/v1/...`), not from the form, because the form once showed a German draft Apple
      had never stored. The full record, and the decisions Ayo made in it, is
      `store-submission.md` §3b "iOS SUBMISSION RECORD".
- [ ] **Slice 8 · Close-out: THE CHECKLIST IS §6.** Everything after Apple's answer, in order,
      with who does it, where, the exact value, and how to prove it worked.

## 4. Open questions to settle inside their slice, not before

- **Is the review bypass still armed on production?** `store-submission.md`'s follow-up 2 (off at
  Play approval + 7 days) is not struck, and `credentials.md` row 17 still says "NOT enabled".
  One of them is stale. Read the secret names with `supabase secrets list` (names and digests,
  never values) before slice 7, and fix whichever record is wrong.
  **ANSWERED 2026-09-14: ARMED.** All three `REVIEW_*` secrets are set on `agbc-production`
  (names read with `-o json` and printed without digests, because a 6-digit code's SHA-256 is
  reversible in a second). So `credentials.md` row 17 is the stale one, and the Play window's
  own deadline (approval + 7, about 2026-09-13) has passed. It stays armed for Play's 1.0.2
  review and Apple's; the fresh-code question is slice 7's, and it has to account for Play's
  App access section holding the current code.
- **Does `aps-environment` come out as `production` in the store build?** Introspection shows
  `development`, which is the plugin's default; distribution signing is expected to set it from
  the provisioning profile. Without an iPhone the only evidence is the built IPA's entitlements,
  which is worth reading once, since a wrong value means no push on iOS with no error anywhere.

## 5. Found along the way

- **The share preview is clipped on every tablet** (found 2026-09-15 in the iPad captures, and
  live in Android 1.0.2 for any tablet). `PreviewWindow` in
  `apps/mobile/src/features/share/SharePreviewSheet.tsx` sizes the frame `width: '68%'`,
  `maxWidth: 280`, `aspectRatio: 1`, and Yoga resolves the aspect ratio from the 68% width
  BEFORE the max-width clamp: on a sheet wider than ~412dp the width is capped at 280 while the
  height stays at 68% (430dp on the Tab S10+ in portrait), and the picture fills that frame,
  losing both sides. A phone never reaches the cap, which is why W4.15's phone matrix passed.
  The shared PNG is unaffected (it is rendered off-screen at full size); only the preview the
  member approves is wrong, which is exactly what `.scwrap`'s own comment says a preview must
  never be. **Fixed in PR #315 (merged 2026-09-14): `maxHeight` capped by the same constant,
  a failing test first, and the arithmetic checked in Yoga itself. It ships in 1.0.3** (Ayo:
  "we will do 1.0.3 later"), so iOS 1.0.2 went to review without it and without the iPad share
  screenshot. Device verification is owed with that build (§6, step 10).
- **Apple refused "AGBC Global" as the German app name** (2026-09-15): "the app name you entered
  is already being used". English (U.K.) had accepted the same name hours earlier. Apple reserves
  a name per LOCALE, no live app of that name exists in Germany or the UK (iTunes search), and
  this team holds exactly one app record, so the most likely holder is another developer's
  unreleased record. Dutch and French were not attempted, expecting the same wall. **Decided (Ayo):
  ship English only.** Every storefront shows the English listing; the app itself still runs in
  all four languages. The German draft was never stored (confirmed in the API) and was discarded.
  Options if localized listings are wanted later, in `store-submission.md` §3b.

## 6. After Apple answers: the checklist

**How to use this.** Work top to bottom. Each step says WHO does it, WHERE, the EXACT value, and
HOW TO PROVE it worked. Tick the box in the same PR that does the step, and write the date beside
it. A step is not done because a console said "Saved": it is done when its proof passes (the
rule `store-submission.md` §SUBMISSION RECORD learned three times over).

**Fill these in as they happen:**

| Event | Date |
|---|---|
| Submitted to App Review | **2026-09-15, 06:38 UTC** |
| Apple's answer (approved or rejected) | ____ |
| Released on the App Store (step 1) | ____ |
| Bypass-off due (approval date + 7 days) | ____ |

**If Apple REJECTS instead**, none of the steps below apply yet. Read the rejection in App Store
Connect > AGBC Global > App Review (Resolution Center), paste it to Claude, and decide together
whether it is a metadata fix (answered in App Store Connect, no new build) or an app fix (a new
build, which means 1.0.3 on both stores, per §1's one-commit rule). The review sign-in stays ON
through a resubmission.

### A. The day Apple approves

Apple emails the team ("The status of your app has changed to **Pending Developer Release**").
The release is MANUAL, so nothing is public until step 1.

- [ ] **1. Release iOS 1.0.2.** WHO: Ayo, or Claude in Chrome on Ayo's go.
      WHERE: App Store Connect > AGBC Global > iOS App 1.0.2 > **Release This Version**.
      What it does: AGBC Global replaces Grace Portal on the App Store for everyone, all at once
      (no phased release), and the star rating resets (chosen at slice 7).
      PROOF: `https://itunes.apple.com/lookup?id=6760579106&country=gb` returns `"trackName":
      "AGBC Global"` and `"version": "1.0.2"`, and `https://apps.apple.com/app/id6760579106` shows
      AGBC Global. Allow up to 24 hours for both to catch up; the version page in App Store Connect
      says "Ready for Distribution" first.
- [ ] **2. Turn on the website's App Store button.** WHO: Claude, as a PR in `Desktop/agbc`
      (`agbc-website`), AFTER step 1's proof passes (before it, the link opens Grace Portal).
      WHERE and WHAT:
      - `src/lib/site.ts`: `appStoreUrl: 'https://apps.apple.com/app/id6760579106'`, and replace
        the "no numeric id yet" comment above it.
      - `src/pages/app/[...rest].astro` lines 20-21: the comment about waiting for the id is stale.
      - `src/i18n/ui.ts`: `app.iphone` ("On iPhone soon.") in all four languages goes unused once
        the URL is set; delete the four keys or keep them, but decide rather than leave them.
      PROOF: on the Vercel preview, then on `https://www.agbcglobal.com/app`, the "Download on the
      App Store" button shows beside Google Play (in `/de/app` too), and tapping it opens AGBC
      Global on the App Store.
- [ ] **3. Record the release.** WHO: Claude, in one docs PR in this repo.
      - `docs/runbooks/releases.md`: the iOS 1.0.2 row's Store column becomes "App Store, live
        <date>".
      - `docs/runbooks/store-submission.md` §3b iOS SUBMISSION RECORD: the approval and release
        dates, and anything App Review said.
      - `docs/spec/18-BUILD-PLAN.md`: tick the Apple line.
      - `docs/runbooks/credentials.md` Apple row: nothing changes unless a key or certificate did.
- [ ] **4. Check the association file reached Apple's CDN.** WHO: Claude. It is what lets an
      iPhone with the app open `www.agbcglobal.com/app/...` links (every share card's QR) in the
      app rather than Safari.
      PROOF: `https://app-site-association.cdn-apple.com/a/v1/www.agbcglobal.com` answers **200**
      with `58MF8H28K5.com.olayinkaademiluka.grace-portal` in the body. It answered 404 on
      2026-09-14, which is normal before Apple's crawler visits. Still 404 a week after release:
      investigate (the live file itself is proven, `Desktop/agbc` `docs/SPEC-app-links.md`).
- [ ] **5. Nothing to do about the update floor, and here is why.** `app_config.minimum_supported_version`
      stays `{"ios": "1.0.0", "android": "1.0.0"}`. The only AGBC Global iOS build that exists is
      1.0.2, so an iOS floor retires nobody, and Grace Portal (the Flutter app iPhones had) has no
      update gate at all. Raise a floor only when a release must retire the one before it, and only
      for the store that already shows it live (`store-submission.md` §4). Tick when read.

### B. Approval date + 7 days: close the review window

The review sign-in is a working key into a member account for as long as it is on. Not at
approval: stores re-check apps after approval, and a reviewer meeting a dead sign-in is a flag.
Seven days is the window `03` sets. Play's 1.0.2 went live on 2026-09-14, so Apple's approval is
the later of the two and sets this date.

- [ ] **6. Turn the review bypass OFF.** WHO: Ayo in PowerShell (Claude can run it on Ayo's go).
      FIRST check the CLI is signed into the church's account, not another: `supabase projects list`
      must show `agbc-production` (`mqvojrkotwwvwzsewybx`). A 403 means the wrong account, not an
      expired token. Then, exactly as `store-submission.md` §1 "Turning it off" says:
      ```
      supabase secrets unset --project-ref mqvojrkotwwvwzsewybx REVIEW_BYPASS_ENABLED REVIEW_EMAIL REVIEW_CODE
      supabase functions deploy review-signin --project-ref mqvojrkotwwvwzsewybx
      ```
      PROOF, both of them: `supabase secrets list --project-ref mqvojrkotwwvwzsewybx` shows no
      `REVIEW_` names (read the NAMES only, never the digests: a 6-digit code's hash reverses in a
      second); AND signing in to the app with the review address and the old code is REFUSED. An
      untested "off" is a belief.
- [ ] **7. Record that it is off.** WHO: Claude, docs PR.
      - `docs/runbooks/credentials.md` row 17: ARMED becomes "OFF since <date>, proven by a refused
        sign-in".
      - `docs/runbooks/store-submission.md` §SUBMISSION RECORD: strike follow-ups 2 and 4.
      - Note in both that Play's **App access** section and App Store Connect's **Sign-In
        Information** still hold the dead code, so the NEXT store review of either needs a fresh
        window per §1, with both consoles updated to the new code.
- [ ] **8. Purge the review account's posts.** The account is `Play Review`,
      `graceportalad@gmail.com` (`store-submission.md` follow-up 3). `03` keeps its posts out of
      every moderation queue, so nothing reached the feed, but they should not sit in the tables.
      **There is no written procedure for this yet, so do not improvise one against production.**
      In that session: Claude lists what the account owns (read-only), Claude and Ayo choose the
      method, it is written into `store-submission.md` §1 first, and only then run. PROOF: the same
      read-only listing comes back empty.

### C. The next release, 1.0.3 (both stores, one commit)

Not gated on Apple, except step 9, which waits for step 1.

- [ ] **9. The app's own App Store link.** `apps/mobile/src/lib/links.ts` line 55:
      `APP_STORE_URL = 'https://apps.apple.com/app/id6760579106'`, and delete the TODO above it
      (today it searches the store for "Grace Portal"). Used by the update wall and the update
      notice (`UpdateRequiredScreen.tsx`, `UpdateNotice.tsx`); `update-gate.test.tsx` and
      `update-notice.test.tsx` import the constant, so they follow on their own.
- [ ] **10. Verify #315 on devices, then recapture one screenshot.** The share preview fix is
      merged but has never run on a device. From the installed 1.0.3 build: open a Share on the
      Tab S10+ in portrait AND landscape and on the S22, and the preview card must be square and
      whole. Then capture the iPad share screenshot:
      `LOCALES="en" STORE=apple bash scripts/capture-store-screenshots.sh` on the tablet, and add
      `ipad-13/en-3-share.png` to the iPad 13" set in App Store Connect (5 of 10, in order).
- [ ] **11. New EAS development builds for both devices.** The S22 and the Tab S10+ now carry the
      store build (the dev clients were uninstalled for slice 6), and #264 changed native code
      anyway. Costs one Android development build; Ayo decides when, against the monthly quota.
- [ ] **12. The 1.0.3 train itself** follows `docs/runbooks/releases.md` from step 1 (version bump,
      tag, both builds from one commit, Play upload, `eas submit -p ios`, a new App Store version
      with its own What's New).

### D. Close out W4.17 (after A and B)

- [ ] **13. Localized App Store listings (optional, Ayo's call).** Blocked on the app NAME per
      locale (§5). Options: a slightly different name in DE/NL/FR ("AGBC Global Church", say), or
      a name claim through App Store Connect's "submit a claim" link. The copy is ready in
      `docs/store/listing.json`. Leaving English-only is a valid end state.
- [ ] **14. Open question from §4**: does the store build carry `aps-environment = production`?
      Read it once from the IPA (EAS build `24f10277`'s artifact): a wrong value means no iOS push,
      silently. Optional until an iPhone exists, and worth doing before anyone relies on iOS push.
- [ ] **15. W4.16 is complete** once iOS 1.0.2 is live: its app shipped on both stores. Mark its
      board entry COMPLETE in `25`, and delete `docs/spec/plans/W4.16-rhythm-real-calendar.md`
      after checking `10` and `02` carry what it decided (its own instruction).
- [ ] **16. W4.17 is complete.** Move what outlives this file into `store-submission.md` §3b and
      `releases.md`, mark the `25` board entry COMPLETE, update the project `CLAUDE.md`
      current-state line, and delete this file.
