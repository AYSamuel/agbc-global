# 24 · Phase -1: Accounts, Access, Lead Times, First Week

Everything that must exist BEFORE Phase 0 can run smoothly. Lead times verified 2026-07-13; amended 2026-07-18 for the email-OTP decision (`03`) and again 2026-07-29 for ADR [0014](../decisions/0014-push-only-broadcasts.md).

> **There is now NO multi-week external fuse left in this project.** Email OTP took Meta off the auth path in July; dropping the WhatsApp Cloud API took it off the broadcast path too. Everything below is same-day, free, and under our own control. Nothing here waits on another company's review queue, and no legal document has to be chased before building.

Items marked **ASK AYO** need information or action only Ayo/the church can provide.

## 1. Accounts and lead times

| # | Item | Who | Lead time | Blocks if late |
|---|------|-----|-----------|----------------|
| 1 | ~~Meta Business Manager + business verification~~ | | | **DROPPED 2026-07-29** (ADR 0014): broadcasts are push + in-app only, so nothing in the product touches Meta. This removed the last multi-week fuse in the project. The church may still want the incorporation certificate and utility bill for its own reasons; the app no longer needs them |
| 2 | ~~Twilio account (upgraded) + Verify service; Geo Permissions, Fraud Guard, spend alert~~ | | | **DROPPED 2026-07-18:** email OTP (`03`) removes Twilio from v1 entirely; revisit only if phone/WhatsApp OTP is added post-v1 |
| 3 | ~~Twilio Verify WhatsApp channel (bring-your-own sender)~~ | | | **DROPPED 2026-07-18** with phone OTP (`03`) |
| 4 | ~~WhatsApp Cloud API broadcast sender number~~ | | | **DROPPED 2026-07-29** with the Cloud API (ADR 0014). The church keeps its existing WhatsApp community; the dashboard just hands a leader pasteable text (`17` §2), which needs no number, no WABA and no registration |
| 5 | ~~WhatsApp broadcast tier ramp~~ | | | **DROPPED 2026-07-29** (ADR 0014). Worth recording why it mattered: unverified was 250 business-initiated conversations/24h and verified 1,000, so a 2,000-member blast was impossible at tier 1. The paid channel could not have reached the ministry for weeks after launch |
| 6 | ~~Nigeria SMS sender-ID registration (NCC pre-registration via Twilio)~~ | | | **DROPPED 2026-07-18:** no SMS channel in v1 (`03`); email has no DND problem, so Nigeria sign-in no longer needs a registration fuse. Revisit only with post-v1 phone OTP |
| 7 | **Apple Developer team**: membership current? fee waiver re-confirmed annually by the Account Holder? Confirm last shipped iOS build number | **RESOLVED 2026-07-13: the church holds the account; Ayo needs an Admin invite from the Account Holder.** Request it day 1 | invite same day; lapsed renewal = days | Everything iOS |
| 8 | **Google Play Console**: add developer user; confirm highest uploaded versionCode (> 19 rule) and the **Play App Signing SHA-256** (needed for assetlinks.json) | **RESOLVED 2026-07-13: Ayo's own account.** Remaining: look up the highest versionCode + the App Signing SHA-256 | same day | Android release + App Links |
| 9 | Firebase project + **FCM V1 service account key** into EAS credentials (required for Expo Push on Android; no Firebase SDK in the app) | Developer | same day | Phase 3 push |
| 10 | **Supabase org access + the region check** | **RESOLVED 2026-07-13: region CONFIRMED `eu-central-1` (EU); org access confirmed; plan = Free.** The gate passed: the reuse-as-prod plan stands. **Pro upgrade deferred by decision**: see the precondition note below this table | done | n/a |
| 11 | **Church website deploy + DNS access** (AASA, assetlinks.json, security.txt on the Astro site; DNS for email SPF/DKIM) | **RESOLVED 2026-07-13: Ayo deploys the site (Vercel, agbcglobal.com) AND holds DNS/registrar.** No external dependency | hours | Universal links, App Links, email sending |
| 12 | **Transactional email, NOW THE AUTH CHANNEL** (`03`): Resend (the website already uses it) wired as Supabase Auth custom SMTP; SPF + DKIM aligned and DMARC at enforcement on the domain (Supabase's built-in sender is 2 emails/hour, dev-only) | Developer, needs #11 | ~1 day once DNS access exists | ALL email-OTP sign-in for real users: must exist before Founding Members / the first real sign-ins (dev uses local Mailpit, so the build itself never waits). Also Payhip restore-purchase (Phase 4) |
| 13 | Payhip credentials + webhook config (no approval process) | **RESOLVED 2026-07-13: the lead pastor's account. The ASK CHANGED on 2026-09-01 (W4.1) and is now narrower: do NOT request the account API key.** Payhip has one key per account, with no scopes and no read-only mode, and it authenticates `POST /api/v2/coupons`, so it can mint free discount coupons on the church's store. Three things instead: (a) the **webhook signature constant**, which is `sha256` of the account key, computed by Ayo and handed over as the digest, so the key never leaves the pastor's account and what we store can verify a webhook and nothing else; (b) one **`product-secret-key`** per book, from that product's own edit page, which is the scoped credential licence verification actually uses (v2); (c) a **test product with licence keys switched on**. Into Supabase Vault / function secrets, never chat or email | same day | Phase 4 entitlements (W4.1, parked 2026-09-01) |
| 14 | Google Cloud project + YouTube Data API key (default 10k units/day is ample) | Developer | same day | Phase 1 Watch |
| 15 | GitHub private repo (**done**: `AYSamuel/agbc-global`), Expo/EAS, Vercel, PostHog EU, Sentry, healthchecks.io, UptimeRobot | Developer | same day each | CI, builds, observability. EAS Free covers week 1; plan Starter ($19/mo) at launch |

> **Supabase Pro deferral (decision 2026-07-13):** the upgrade is deferred for now. Consequences while on Free: production (which serves the LIVE church website today) has no automatic backups, 5GB/month egress, and 200 realtime connections. ~~**Hard precondition:** the nightly off-provider `db dump` pipeline + one verified restore MUST exist before ANY destructive step of the Grace Portal cleanup (`19`).~~ Both exist since 2026-08-10, and there is no cleanup left to gate (ADR 0023); the pipeline's remaining job is the archive taken before the old project is deleted. Pro remains a pre-launch requirement regardless (Free's egress cannot serve sermon audio; see `21` §9). ~~**Traffic fence (hard rule):** no app build may point at the shared prod project while it is on Free.~~ **LIFTED 2026-08-17 (ADR 0023), and the math below is why the replacement is not a loosening.** The 5GB figure is ~100 full sermon-audio plays TOTAL, so the fence was really a fence around ONE thing: sermon audio, which is a private bucket written only from the dashboard. Production therefore gets **no sermon audio at all** during this phase, which `08` already handles as a state (the audio toggle is absent with a tooltip, not broken), and with that the app's pull from Supabase is JSON plus kilobyte-sized artwork. Video was never in the count: it streams from YouTube. What replaces the fence: usage alerts at 80% on egress, database size and storage (`21` §6.6 has asked for these since the beginning and they have never been set), a ceiling on testers, and a written trigger, **past 50% egress in any month, stop and upgrade to Pro.** Founding Members still runs on Pro or not at all. **Pro remains a precondition of public launch**, and the reason the fence could be lifted at all is that production is now a project of OUR own (a Free-tier restriction no longer takes the church website down with it, since both live there together only after Phase 3 and the same audio rule protects both).

## 2. Decisions that block the first commit (proposed defaults)

1. **Repo (RESOLVED 2026-07-13; AMENDED 2026-07-18)**: private repo `agbc-global` under **Ayo's personal GitHub account** (Ayo's call; the free-org option was considered and declined). Enforced branch protection and reviewer-gated environments need a paid tier on private repos, so until GitHub Team (~$4/user/month) is justified by a second contributor: prod deploys run via a manually triggered `workflow_dispatch` job (the solo-dev equivalent of the approval gate) and branch discipline is by convention (see `23` §4).
2. **Versions**: Node LTS pinned in `.nvmrc` + `engines`; pnpm pinned via `packageManager`. Corepack is being removed from newer Node distributions: install pnpm standalone.
3. **Lint/format**: ESLint flat config (`eslint-config-expo` + `next/core-web-vitals` + `typescript-eslint` strict), Prettier with `singleQuote: true`, shared config package.
4. **apps/mobile internal structure** (satisfies the frontend-bootstrap stages): `app/` = Expo Router routes only, thin files (`(tabs)/home|watch|family|give|more`, `auth/`, `sermon/[id]`, ...); `src/features/<feature>/` (components, hooks, api per feature); `src/components/ui/` (the `05` primitives: Button, Card, GateSheet, Skeleton, EmptyState, Toast...); `src/lib/` (supabase client, analytics, storage); `src/theme/` (re-exports tokens); `src/i18n/`; `src/state/` (Zustand stores incl. gate-return); `assets/fonts/`.
5. **i18n layout**: `src/i18n/locales/{en,de,nl,fr}/{common,home,watch,family,give,events,church,auth,settings,errors}.json`; i18next namespaces = filenames; keys mandatory from Phase 1. (`church` added at W1.7 for the screens `04` owns directly: BRANCHES, BRANCH-INFO, ABOUT, CONTACT.)
6. **Design tokens as code**: `packages/shared/src/theme/tokens.ts` exporting typed `light`/`dark` objects with the exact `05` values; consumed by BOTH the mobile theme provider and the dashboard Tailwind config so app and dashboard cannot drift.
7. **Project CLAUDE.md** for the monorepo, containing: the stack + versions; the command list; the app identity block (`com.oami.agbcapp`, versionCode >= 20, existing keystore, `com.olayinkaademiluka.grace-portal`; never regenerate credentials); the **FENCED SUPABASE OBJECTS list** (placeholder until the `19` audit fills it; no migration/policy/GRANT may touch them); conventions (i18n keys only, four data states, guest-first gates, server-trusted writes, tokens only, no em-dashes); the secrets map pointer to `21` §3 / `23`.

## 3. Windows 11 dev machine

Docker Desktop with WSL2 backend (required for `supabase start`; if ports 54321-54329 collide with Hyper-V's reserved ranges, check `netsh int ipv4 show excludedportrange protocol=tcp` and remap in config.toml); Node via nvm-windows; pnpm standalone; Android Studio (bundled JDK, SDK 36, emulator) + the physical Android; `eas-cli`, `supabase` CLI (Scoop), Deno 2.x, GitHub CLI. iPhone: `eas device:create` BEFORE the first dev build, and enable Developer Mode on the phone (Settings > Privacy & Security) or the install will not launch. Optional: Windows Defender exclusions for the repo + Metro cache.

## 4. First-week execution order

- **Day 1: access audit (no fuses left at all).** The ASK-AYO sweep: Apple Admin invite requested, Play console versionCode + App Signing SHA-256 looked up, Payhip owner, website/DNS owner. Nothing to chase from Meta any more (ADR 0014). Repo already exists (`AYSamuel/agbc-global`); run the remaining `23` §4 bootstrap steps.
- **Day 2: machine + skeleton.** Dev environment (§3); monorepo scaffold + CLAUDE.md + lint config + empty pr.yml; dev Supabase project (EU); PostHog/Sentry/healthchecks/UptimeRobot accounts.
- **Day 3: app identity + credentials.** create-expo-app (Router, TS strict); identity values + versionCode 20; fonts + tokens.ts; EAS project + profiles; upload the existing keystore to EAS; Apple signing via EAS; `eas device:create`; kick off both development builds; Firebase project + FCM key into EAS.
- **Day 4: local backend.** `supabase init` + `supabase start`; begin the `02` schema as migrations against local; seeds skeleton; wire the supabase CI job (migrations + pgTAP) + mobile typecheck/lint/Jest.
- **Day 5: green exit.** Dev builds installed on both phones with hot reload from Windows; dark/light tokens render; CI fully green on the empty app; buffer for surprises. Week 1 exit = Phase 0 exit minus prod work, which correctly waits for the region decision, the audit, and the rehearsal (`19`).
- **Still in flight after week 1 (by design):** nothing external. The Meta verification, sender registration, display-name review and tier ramp that used to sit here all left with ADR 0014. Email OTP has NO external fuse either: local dev uses Mailpit, and the Resend SMTP wiring (row 12) is a ~1-day task due before the first real sign-ins.
