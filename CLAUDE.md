# AGBC Global · Project Instructions

Mobile app (iOS + Android) + leader web dashboard for Amazing Grace Bible Church: a multi-branch, diaspora-shaped ministry (Glasgow HQ, Berlin, Emmen, Ogbomosho). The wedge is "belonging made visible": testimonies, the prayer loop, and the global family map.

## Current state

**Derive the real position from git history + the board every session (`25` §1). This section
is an orientation, not a source of truth, and it goes stale between work items: it sat on
"no app code yet" until 2026-08-11, well after the whole of Phase 1 and most of Phase 2 had
shipped. If you find it wrong, correct it in the same change.**

- The complete spec lives in `docs/spec/` (docs 00-25; the old `.dc.html` prototypes in `docs/spec/prototypes/`, ADRs in `docs/decisions/`, runbooks in `docs/runbooks/`).
- **The code exists and is substantial.** `apps/mobile` (Expo Router: every Phase 1-2 screen plus W3.1's player, MY-LIST and SERMON-NOTES), `apps/dashboard` (Next: moderation, people, reports, verses, sermon audio + artwork), `packages/shared`, `packages/config`, and `supabase/` (75 migrations, 52 pgTAP files, 17 edge functions, seeds, snippets). `pnpm test` runs 880 mobile + 296 dashboard tests, `pnpm test:db` 1525 pgTAP assertions across 52 files, `deno test` 175 (counts refreshed 2026-08-31); six Maestro E2E journeys run on the Android device (`apps/mobile/maestro/`, not in CI yet). Assume a surface is already built and go read it before planning to build it.
- **Phase 2 has exited (2026-08-14).** All ten W2 items plus the exit audit are done: members contribute, the Family loop works end to end, and nothing publishes without approval (proven by Maestro journeys + publish-bypass attempts refused at the app and RLS layers). Note two things a signed-in member still sees that are NOT Phase 2 misses: the More tab renders its guest sign-in card (the member "My life" section is scheduled for W3.3, `25` line 252; W1.2 built the hub with the card standing in), and the iOS E2E is owed (no iPhone in the project yet). Both are on `18`'s launch checklist.
- **W2.10 (#136) is done.** Analytics (17 events, opt-in, PostHog EU) + Sentry across app/dashboard/edge + the exit audit. ADR 0020 carries the decisions.
- **W2.9 (#135) is done.** Slices 1-3 shipped (RSVP, the Academy pathway, the shared `course_registrations` handoff per ADR 0017). Its last piece, the "add another email" claim surface, was **cut on 2026-08-11** (ADR 0017 amendment) and its backend removed with it. Consequence to know before touching the Academy: a member who paid on the website under a different address than their app account has NO path until the dashboard's leader-linking tool exists (#164), and can pay twice. The interim is manual.
- **PHASE 3 HAS EXITED (2026-08-31). W3.6, its exit audit, is COMPLETE and Phase 4 is next.** The audit had to BUILD before it could verify: **three notification kinds `09` promises had no producer at all**, so `prayer_activity` and `testimony_activity` were two NOTIF-PREFS switches that gated nothing for six weeks. Slice 2 built `activity-notices` (migration `20260829120000`), live in production and monitored. All ten tiers were then driven to the physical Android device AGAINST PRODUCTION: right words, right channel (all six), right destination (all four deep-link routes), from both backgrounded and killed, with branch-scoping proven live across two branches. The audio clause was field-tested by Ayo and REPORTED rather than machine-checked. **Phase 3 exits without, deliberately:** iOS for both audio and E2E (no iPhone), and five of six Maestro journeys, which cannot run in CI because the review bypass is off on production (the blocker is authentication, not CI minutes). Both on `18`'s checklist. Six lessons are on `25`'s W3.6 entry; the three that generalise furthest are that **a green test is not evidence until it can fail** (mutation-check new assertions), that **a function without a `[functions.<slug>]` block in `config.toml` is a job that no-ops silently in BOTH environments** while `cron.job_run_details` records "succeeded", and that **after any write through a tool you do not control, ask the system what it now holds** rather than trusting the command's output.
- **Phase 4 has begun, and W4.0 (#164) is COMPLETE (2026-08-31).** A member who paid on the website under one address and signed into the app under another had NO path at all, and the double-booking wall could not help because it keys on `(course_id, profile_id)` and their row has no `profile_id`: they could pay twice. Backend: migrations `20260831120000` (two columns, four admin-only definer routines, trigram suggestions) and `20260831130000` (a FOURTH `activity_notice_batch` arm, which finally gives `registration.confirmed` a producer). Screen: ten approved frames, then `/academy` (three views on one route), `/academy/[id]/link` (four surfaces on one route: suggestions, search, confirm, refusal) and `/academy/[id]/unlink`, all working with HTML alone. **Two rules are enforced by the loader rather than by the markup**: the amount is absent from `REGISTRATION_COLUMNS` so it is never read at all (`20` §minimum necessary), and `set_aside_by` could not be read even if asked for. Three lessons worth carrying: **a green test is not evidence until it can fail** (the first "amount is never fetched" test passed with the column added back, because it was testing the mapper while claiming to test the query; and pgTAP `052` asserted `null` messages while the dashboard mapped on those exact strings); **the four defects that reached a screenshot were all copy or layout**, invisible to 326 passing tests and obvious on screen (a refusal naming the payer instead of the member, one sentence printed twice, a hint repeated as its own error, a filled button endorsing the top suggestion); and **the search that looked like new backend needed no migration**, because `20260802120000` already gives admins SELECT on every profile under RLS. That search is a deliberate, narrow departure from `17` §5's exact-email rule, recorded there. **A fourth lesson came from CI rather than the build**: `course_registrations` had never granted `service_role` by name, inheriting Supabase's bootstrap grant instead, and the pinned CI CLI does not hand that out, so every service-key INSERT failed there while passing locally. `20260831140000` grants it explicitly (as `donations` always did) and `039` asserts it. **An ambient grant is not a grant this repo owns**, and this one sat unnoticed on a table the live website's Stripe webhook writes to.
- **The leader dashboard is DEPLOYED (2026-08-31) at `app-dashboard.agbcglobal.com`.** Vercel project `agbc-dashboard`, Git-connected to `apps/dashboard`, redeploys on push to `main`; env carries public values only, and `SUPABASE_SECRET_KEY` is deliberately absent because `createAdminClient()` still has zero callers. Until this, no leader could moderate anything without somebody running `next start` on a laptop, and **nothing in `18` had ever asked for it to be deployed**. Found by auditing the launch checklist against reality.
- **W3.5 is COMPLETE (2026-08-22).** All of W3.5 landed: broadcasts with their four-eyes approval and resumable fan-out, the events module with cancellation/reschedule notices, the event picture (`event-images`, a public-read bucket a branch's own LEADERS may write, per `02` §Storage), and branches in three PRs (the domain and `archive_branch()`, the dashboard module and the one-HQ rule, and the member's re-home prompt). Four lessons from it are on `25`'s W3.5 entry; the one that generalizes furthest is that **`02` saying something "stops" is worth checking caller by caller**: "an archived branch's services stop" turned out to describe one job's JOIN, so the app went on advertising a closed branch's Sunday service with a check-in beneath a banner saying it had closed. Also from it: PR #207 closed issue #96 by revoking Supabase's ambient table privileges across ten tables and three views, so a column grant can finally bite.
- **W3.4 (the reminder jobs) is COMPLETE (2026-08-19).** Six schedules now ride ADR 0016's mechanism: `service-reminders` (every 15 min), `rsvp-reminders` and `prayer-reminders` (hourly), `counter-reconcile` (nightly), `retention-purge` (monthly) and the weekly Resend canary on `verse-monitor`'s Monday pass. The piece nobody had noticed was missing: **W3.3 built the push sender and never built a caller**, so W3.4 slice 1 added `deliver_notifications()`, the seam where a batch of "who is due" becomes rows written and then pushed. **The insert IS the claim on a send** (the partial unique on `(profile_id, dedupe_key)`, ADR 0022), so it comes BEFORE the push and a failed push degrades to notification-centre-only, which is what `15` already promises a member whose push is off. **The pref gate lives in each job's batch SQL, on the column `15` names for that tier, and `allowedByPrefs` is deliberately not called by the jobs**: two gates on one fact would be two owners, and they already disagree (the `prayer` channel routes to `prayer_activity`, while the reminder tier is gated on `prayer_reminders`). Three lessons that generalize past this item: a window computed from `now()` LOSES an occurrence whenever a tick runs late, so every reminder window floors the clock to the tick grid and every batch function takes its clock as an argument (which is what made DST testable from both sides in August); a pgTAP file that counts rows out of LIVE STATE is only a test if the state is its own (`038` and `041` both went red the moment real callers and real seed enrolments existed); and a trigger function runs as the INVOKING role, so a helper the guard calls must be granted to `authenticated` or the member's own tap fails `42501`.
- **W3.1 (the audio slice) is COMPLETE, all five slices landed:** the private `sermon-audio` shelf and its dashboard upload (1a, 1b), the native rebuild carrying `expo-audio` + `expo-notifications` (2), the background/lock-screen player with server-synced resume (3), the controls redesign, Save + MY-LIST + SERMON-NOTES (4), and message artwork (5: the public-read `sermon-artwork` bucket, `artwork_path`, the picture on all three dashboard surfaces and all four app surfaces that draw one). Its slice plan was deleted on landing per its own instruction; what it decided now lives in `02` (both buckets and both columns), `08` (media architecture and the YouTube ToS box), `17` §4, ADR 0015-0020, and each migration's own header. W3.3 owns push (and the six deferred analytics events + the two blocked Sentry DSNs land with their infrastructure, see `docs/runbooks/credentials.md`).
- **Two buckets, two postures, and the difference is the OBJECT rather than the neighbour** (W3.1, 2026-08-14 and 2026-08-15). `sermon-audio` is private with 24h signed URLs because it holds the asset; `sermon-artwork` is public-read because it holds the advertisement for it, shown on every card in a guest-first app. Before copying either posture to a third bucket, ask what the object IS: `02` §Storage carries both arguments, and the costs that decided the second (image caching by URL, signed URLs that must never be persisted, and the OS fetching lock-screen art out-of-process) are exactly the ones a "make it private like the others" instinct would miss. `public` widens only the read path; writers stay live-table admins at `aal2` either way.
- **Track P: Phases 0-4 are DONE (2026-08-19); only Phase 5 (delete the old project) remains.** Phase 4 pointed the app at production (EAS preview env carries the `sb_publishable_` key; the preview build is verified against production on the device), onboarded BOTH admin accounts (promotions + NULL-actor audit rows confirmed, TOTP enrolled for both via the dashboard run locally against production, break-glass checklist complete), closed W3.3 slice 4's tap caveat (the notification tap works from killed and backgrounded; the dev-client conflict was the artefact), and proved the Academy handoff end to end (`COURSE_HANDOFF_ENABLED=true` live; a sandbox checkout landed a row with `link_method=handoff` linked to the member). It also found and fixed a Phase 1 seed gap: `05-courses.sql` had never been applied (only `00-common` was), so the production Academy was empty until 2026-08-19. **W3.3 is now COMPLETE (slice 5 landed 2026-08-19):** NC, NOTIF-PREFS, MORE's member "My life" hub and the Home bell, verified on the device in both themes, at max font scale on a narrowed width, and in German with the longest branch name. Two lessons from it that generalize: the OS-denied banner keys off "we have already had our one ask" plus "not granted", never `canAskAgain` (a revoked, even user-fixed, Android permission still reports it true outside an in-process request), and a PostgREST keyset cursor must URL-encode the `+` in its timestamp or Postgres reads a space and answers `22007`. The item's slice plan was deleted on landing per its own instruction; what it decided now lives in `15` (channel importance, the shared prayer control, the banner condition), `25`'s W3.3 entry, ADR 0022 and the migrations' headers. The plan in `docs/spec/plans/track-p-fresh-prod-project.md` remains authoritative and carries each phase's execution notes. Production is fully operative: nine functions deployed with secrets, four cron jobs alive and pinging healthchecks, Watch synced (100 sermons, no audio, per the fence), auth audited against `config.toml`, and **the live website records donations and registrations into `agbc-production`** (verified with real test-mode rows + thank-you emails). **ADR 0024's new-API-keys migration landed EARLY, during Phase 2** (migration `20260819100000`): the platform's provisioning-time env copy of the legacy service-role key turned out stale, unrecoverable and unrotatable, so the job path runs on `sb_secret_` keys (vault `secret_key`, `apikey` header, `verify_jwt = false`, caller auth in `_shared/auth.ts`); the ADR's amendment holds the evidence. The app still uses legacy keys until Phase 4 hands EAS the `sb_publishable_` key; never disable legacy keys before that.

## How to work in this repo

1. **`docs/spec/25-BUILD-PROCESS.md` is the execution playbook.** Every build session follows its session protocol (§1): derive position from git history + the board, pick the next work item, read its Refs, build the slice BE-first, verify, propose the commit. Do not build outside the work-item flow.
2. **The numbered specs win every conflict** about behavior, data, or scope. Prototypes and mockups win on look and feel only.
3. **Design source of truth:** `docs/spec/design/mockups/entry-flow.html` (all screens, light + dark, tablet, edge states). Figma is parked. `design/SCREENS-CHECKLIST.md` is stale; trust `05` + the HTML.
4. Read the matching `~/.claude/standards/` file(s) before the first session in a domain (backend, database, security, frontend, mobile, qa-testing, devops).

## Stack (decided; see `01`)

- **Mobile:** React Native + Expo (SDK 56+, managed, EAS Build), TypeScript strict, **Expo Router only** (never import `@react-navigation/*`), TanStack Query, Zustand, `expo-audio` (never `expo-av`), `react-native-svg` map, i18next (EN/DE/NL/FR), react-hook-form + zod.
- **Backend:** Supabase (Postgres + Auth email-OTP via Resend custom SMTP, typed code, never magic link + Storage + Realtime Broadcast + Edge Functions). RLS everywhere, `FORCE ROW LEVEL SECURITY`, write-path invariants per `02`.
- **Dashboard:** Next.js on Vercel, same Supabase; centralized authz middleware on every server route. Next 16 ships its own current docs at `node_modules/next/dist/docs/`; consult them before dashboard work (its APIs move faster than training data).
- **Tooling:** pnpm workspaces, ESLint flat + Prettier, Jest/jest-expo + RNTL, Vitest, pgTAP, deno test, Maestro E2E.

## App identity (NEVER change or regenerate; see `19`)

- Android: `applicationId = com.oami.agbcapp`, `versionCode` >= 20, signed with the EXISTING upload keystore (in EAS credentials; never in the repo, never let EAS generate a new one).
- iOS: `bundleIdentifier = com.olayinkaademiluka.grace-portal`, existing App Store Connect record, existing Apple team.
- This app replaces Grace Portal on the existing store listings. Do not create new app records.

## Commands (from repo root)

- `pnpm typecheck` · tsc --noEmit across mobile, dashboard, shared
- `pnpm lint` · ESLint (flat, typescript-eslint strict) across mobile, dashboard, shared
- `pnpm test` · Jest (mobile) + Vitest (dashboard)
- `pnpm test:db` · pgTAP via `supabase test db` (local stack must be running)
- `pnpm format` / `pnpm format:check` · Prettier (singleQuote); docs/, supabase/, *.md, and assets are excluded
- `supabase start` / `supabase stop` · local stack (ports remapped to 553xx, see supabase/config.toml)
- `pnpm db:reset` · ALWAYS use this instead of bare `supabase db reset`: it resets local AND re-runs the YouTube sync (sermons are synced, never seeded; a bare reset leaves Watch empty and the app half-featured on device). `pnpm db:sync-sermons` runs just the sync.

## Supabase environments (ADR 0023, `docs/spec/plans/track-p-fresh-prod-project.md`)

- **Production is `agbc-production`, ref `mqvojrkotwwvwzsewybx`**, eu-central-1, Free, created 2026-08-18 with all 56 migrations applied and seeded. This REVERSES ADR 0001 and with it most of `19`'s reuse plan and `24` §1's traffic-fence line; read the plan, not those, for what happens next.
- **The old shared project** (`fotfplvqsnmbzjjhqlwp`) is **PAUSED**, not deleted: the Free tier allows two ACTIVE projects across every org you own and the other slot is a different company's, so `agbc-app` had to yield one. Paused keeps its data restorable for a year; it was dumped immediately beforehand (`nightly/agbc-prod-2026-08-17.tar.zst.age`). **The website moved off it on 2026-08-19 (Phase 3): giving and registrations record into `agbc-production` now.** Deletion is Phase 5. Reversal now costs an extra step: restoring it needs a free active slot, so pause `agbc-production` first.
- **Never `supabase config push` to a hosted project.** `supabase/config.toml`'s `site_url` and `additional_redirect_urls` are `127.0.0.1:3000`; pushing them points production's auth at localhost. Mirror auth settings by hand (`23` §1).
- **The traffic fence is lifted deliberately.** App builds point at production on Free, because the egress risk is sermon audio and it is ours to control: **no sermon audio is uploaded to production storage**, usage alerts sit at 80% on egress/database/storage, and past 50% egress in any month we stop and upgrade to Pro.
- **Destructive-work gate (still a hard rule, with a smaller subject):** the cleanup it guarded is gone, but deleting the OLD project happens only after the website has run on the new one for days and a final dump is archived.
- Daily loop is LOCAL (`supabase start`). The migrations folder IS the schema; never change a hosted project directly.

## THE TWO SHARED TABLES (the fence dissolved 2026-08-17, ADR 0023)

**There is no fence any more, and no CI fence-guard.** Both existed because `donations` and `course_registrations` lived in a project we SHARED with the live website and could only damage. Production is now a project of our own, `donations` is created by our own history (`20260817120000`), and a rule forbidding us to name it would forbid the migration that makes the website work. The old project's inventory, its three measured traps and its ordered cleanup are HISTORY now, preserved in `docs/runbooks/prod-audit-2026-07-30.md` as the record of what is being deleted, not as instructions.

What replaced the fence is narrower and covers more. The risk was never the NAME, it was the SHAPE: **`Desktop/agbc`'s Stripe webhook inserts into both of these tables, on its own release schedule, with no compile step between the two repos.** So the rule is a column contract, and it is asserted in `supabase/tests/039` rather than greped, which for the first time catches a retype or a new NOT NULL as well as a drop.

| Table | The contract |
|---|---|
| `public.donations` | **19 columns, ours since `20260817120000`.** The website only ever INSERTs, with the service-role key, and reads nothing. Holds donor PII (`donor_name`, `donor_address`, `email`). FORCE RLS, ZERO policies, and nothing granted to `anon` or `authenticated`: issue #96's blanket grants over donor records never exist here. `user_id` is ON DELETE SET NULL, the fix for the old project's un-deletable auth users |
| `public.course_registrations` | **ONE table shared with the website** (ADR 0017), ours since `20260809202000`. Additive only: none of `course`, `format`, `full_name`, `email`, `city`, `country`, `branch`, `amount`, `currency`, `payment_status`, `stripe_session_id` may be dropped, renamed, retyped or made NOT NULL without a coordinated change in `Desktop/agbc` |

**The failure this guards against is one-sided, which is why the contract is stricter than it looks.** A column the website omits, made NOT NULL here, fails every insert on the LIVE site while every test in this repo stays green. And a refused insert is not a validation error anyone sees: `insertDonation` treats only `23505` as benign and throws on anything else, so Stripe retries into the same wall and the end state is a donor who has been charged with no record of the gift and no thank-you email. That is also why neither table carries value CHECK constraints on the website's columns, deliberately and against this repo's usual habit: see the header of `20260817120000`.

Two rules that outlive the fence, both from ADR 0017 and ADR 0015: an UNLINKED website registration is visible to **admins only**, never to branch leaders, and `profile_id` plus the link trio are server-written only, because a policy that reads a column makes its subject unable to write it.

**Everything else pre-existing in prod belongs to the retired app and is a drop candidate**, staged behind Track P's backup gate: 13 tables, 48 functions, 21 triggers, 42 policies, 1 view, 6 active cron jobs, the `avatars` bucket, 8 auth users, and the 35-migration history from Jan-Feb 2026.

## Conventions (enforced in review)

- i18n keys only; no literal UI strings in components (EN/DE/NL/FR namespaces; FR is net-new, the website has no French strings).
- Every data surface implements all four states: loading, empty, error, offline. While loading, primary actions that operate on the loading data are hidden, not disabled (no dead buttons under skeletons; decided 2026-07-20).
- Guest-first: browsing never requires auth; contribution gates via GateSheet + gate-return.
- Server-trusted: RLS + triggers are the mechanism, never the client or the UI.
- Authority-bearing columns are part of the security boundary (ADR 0015, W2.7): if an authorization check READS a column, the subject of that check must not be able to write it. `can_moderate_branch()` reads `profiles.branch_id`, so a leader who could edit their own branch could moderate any branch, and every scoping test still passed because each held the leader still. When authority derives from a column, the test matrix must include CHANGING that column, not just acting from each side of it. Related: read authority from the live table, never a JWT claim (`caller_is_admin_live()`, never `is_admin()`), and remember RLS is row-level, so a private column on a row someone else may read needs a different home, not a cleverer policy.
- A private column on a row its subject may read is a GRANT problem, not a policy problem (2026-08-03). RLS decides WHICH ROWS come back and never which columns, so "the author reads their own post" hands over every column on it: `moderation_note`, the safeguarding reason a removal must not disclose, was readable by the author it was written about from W2.7 slice 3 until `20260803140000`. Column privileges are the mechanism (`revoke select on <table>`, then `grant select (col, col, …)`, since a table-level grant cannot be partially revoked), and the whole inventory is asserted in pgTAP so the next column added is a decision rather than an oversight. Two limits worth knowing before reaching for it: every human here is the same `authenticated` role, so a column revoked from members is revoked from leaders too (that is why `moderated_by` stayed and needs a definer read path instead), and a privilege proven in pgTAP still has to be proven through PostgREST, which is a different road (see the safeupdate note in `20260803120000`).
- On a member-owned table with nothing else to force, IDENTITY IS NOT AN INPUT (W3.1 slice 4, `20260815120000`). Three ways to own a row now exist here, and the choice is deliberate: RLS alone (W1.3) lets a client CLAIM an identity and then checks the claim; a guard trigger overwrites `profile_id` where it is already forcing other columns (`rsvps`); and where there is nothing else to force, `profile_id` DEFAULTS to `auth.uid()` and the INSERT grant EXCLUDES the column, so the client cannot even name it and a forgery is refused at the grant layer before RLS is consulted. Prefer the third for new personal tables: a trigger that exists only to assign one column is a trigger to maintain. Two consequences to remember: trusted paths (seeds, service role, pgTAP setup) must name `profile_id` explicitly, and a client that sends it gets `42501` rather than a silent overwrite, so the app's own writes must stop sending it too.
- A privilege proven in pgTAP is not yet proven through PostgREST (W3.1 slice 4, extending the `20260803120000` safeupdate note). pgTAP speaks SQL; the app speaks PostgREST, which SETs every payload column on a merge-duplicates upsert and needs the conflict target to resolve. Both roads were driven for `sermon_notes` and `saved_items` before the screens were built: `035` for the matrix, and real signed-in HTTP calls for the exact request shapes supabase-js sends.
- Privileged actions are audit-logged by a trigger at the data layer, not by each caller (`privileged_actions`, ADR 0015). A caller that has to remember to write the audit row is a caller that will forget. Watch for the actions that change no row and so fire no trigger: those write their own.
- Scheduled work is one pattern (ADR 0016, W2.7 slice 5): `cron.schedule` in the job's own migration calls `jobs.invoke_edge_function('<slug>')`, which reads `project_url` + `secret_key` (the `sb_secret_` key, sent as `apikey`; ADR 0024, since 2026-08-19) from the vault at call time, so the migration is identical in every environment and an unarmed database no-ops. Every job takes a lease and RELEASES it when it finishes (the expiry is only the net under a run that died), derives its work from live state rather than an outbox, records what it sent only AFTER sending, and ends with its healthchecks ping, pinging FAILURE when it could not deliver. Never a GitHub Actions cron (CI budget), never a schedule that lives outside the repo. Local: `pnpm db:reset` arms it; `pnpm jobs:arm-local` re-arms.
- Design tokens only (from `packages/shared` tokens); no hard-coded hex; both themes always. Token values come from the mockup's CSS variables verbatim (never from 05's prose tables; if they disagree, the mockup wins and 05 gets synced in the same change).
- Mockup-first, first-hand: before writing any screen's code, read that screen's frame (HTML + CSS) directly from `docs/spec/design/mockups/entry-flow.html` in the build session. Summaries of the mockup, in ANY form (a subagent's report, 05's prose, memory of the frame), are research input, never the build reference; W1.1, W1.5, and W1.6 all drifted exactly this way. Every screen is then diffed element-by-element against the frame's actual CSS (colors, type sizes, gradients, static vs data-driven regions, selected/edge states) before it counts as done; a behavioral walkthrough is not visual verification. If a surface has no mockup frame (Figma is parked), the mockup comes FIRST: compose the missing frame in `entry-flow.html` from the mockup's existing classes/patterns, get Ayo's approval of the frame, and only then build the screen from it (rule added 2026-07-26; never build a screen straight from imagination or spec prose). **A SURFACE IS A STATE, not a screen** (added 2026-08-08): a guest, empty, or edge variant needs its own frame just as much, and three of them reached the app without one (MY-POSTS guest at W2.6, RHYTHM guest and its none-with-a-milestone variant at W2.8) precisely because a variant does not feel like a new screen while you are building it. Loading, error and offline are the exception: `STATE · loading (skeleton)`, `STATE · error / retry` and `STATE · offline` frame those once for the whole app.
- One visible fact, one owner: any value on screen has exactly one source. Deriving a displayed value from two stores that update independently is the bug, not an implementation detail (W2.4 cost four device-only bugs to this, all invisible to green unit tests). A tap writes to that one source; delivery, retries and offline replay are a separate concern that never becomes a second source of truth.
- Cleanup that touches a NATIVE object must be guarded, because `useFocusEffect`'s cleanup also runs on unmount (W3.1 slice 3, 2026-08-14). Blur and unmount share one seam, and by unmount the native object is usually already gone: `expo-audio` releases its player from a hook that registered its effect FIRST, so its cleanup runs first, and our `player.pause()` afterwards threw "Cannot use shared object that was already released" and took the screen down on every back-press out of the player. Wrap the call in a try/catch with the reason written beside it (`apps/mobile/src/features/watch/useSermonAudio.ts`), and make the guard load-bearing in the suite by MODELLING the release in the test double (`apps/mobile/src/test/expoAudio.ts`: calls into a released player throw, and the fake registers its release effect first, exactly as `useReleasingSharedObject` does). A stand-in that never dies proves the guard unnecessary, which is how this reached the device in the first place. Applies to every native handle on the blur/unmount seam, W3.2's LIVE player and W3.3's push subscriptions next.
- Dashboard (from W2.7 slice 1): every server route awaits `authorize()` in `apps/dashboard/src/server/authorize.ts`; `proxy.ts` only redirects signed-out visitors and is never the check. State-changing route handlers call `isSameOrigin()` first (cookies authenticate, so CSRF needs an explicit defense). Copy lives in `src/copy/en.ts`, not i18next: the dashboard is a staff tool and no spec asks for it to be translated, but no component holds a literal string either. Type sizes are rem, derived from the shared token scale, so a reader's own font setting still works; colours and radii come from `src/theme/cssVariables.ts` (generated from `packages/shared`, no hex anywhere). Tests: two Vitest projects, `server` (Node, real tokens from the local stack) and `ui` (jsdom + RTL + axe), split by `.test.ts` vs `.test.tsx`.
- Never assert focus that a FRAMEWORK placed (React `autoFocus`, a router restoring position): it can land and then be dropped again before the next line, which passes locally and fails on a loaded CI runner. Cost two red builds on W2.7 slice 1. Put focus where the test needs it (`await user.click(el)`), assert that precondition, then test the thing you actually mean, usually tab order. Focus that OUR code sets (an error handler moving focus to the field) is fair game, because we control when it happens.
- Grace-framed copy: encourage, never shame (especially streaks).
- Text-scale verification is data-dependent (lesson from #76, 2026-07-26): always test at the DEVICE'S MAXIMUM font setting (not a nominal 200%) with the LONGEST seeded branch name selected (AGBC Lighthouse Berlin), because overflow only appears when long data meets large scale. Control labels (tab bar, segmented controls, pills) cap at ~1.3x and stay single-line per the 05 rule; body text scales fully.
- Multi-branch: nothing assumes a single branch or hard-codes Glasgow.
- Dev-client native fence: the dev builds on the physical devices contain only the native modules linked at their last EAS build (W0.11 as of W1.6). Importing a newly added native module (e.g. expo-clipboard) crashes the route on those clients. When adding one: guard the import so screens degrade until a rebuild (Gradient.tsx and CopyRow.tsx are the patterns), state in the PR that new dev builds are needed, and get Ayo's go-ahead for the EAS builds.
- No em-dashes in any output, file, or message.

## Git

- Never commit or push to `main`; branch per work item (`feat/w<item>-<slug>`), PR into `main`.
- Every commit AND push needs Ayo's explicit approval first (message proposed, approval never carries over).
- PRs: Claude opens them directly with `gh pr create` after the approved push (title, full body, `Closes #N`), overriding the global prefilled-link rule (decided 2026-07-19 after three PRs lost their bodies to the compare-banner path). Ayo reviews and merges ONLY after the checks are green; Claude watches the run and gives the explicit merge signal.
- No AI attribution anywhere: no Co-Authored-By, no "Generated with" lines, in commits, PRs, or branches.
- Secrets: see the map in `21` §3 / `23` §2. Never in git; the keystore, FCM key, and APNs key live in EAS credentials only. If a secret ever lands in code or git history, stop and rotate it.
- CI budget (hard rule): GitHub Actions minutes are a shared pool across ALL of Ayo's private repos. Workflows must be path-filtered, cancel superseded runs, and cost ZERO when idle: no scheduled runs without a real paying job, prefer platform schedulers (pg_cron, Supabase cron) over Actions crons, and docs-only PRs must trigger no jobs.

## Key references

- Build flow + gates: `docs/spec/25-BUILD-PROCESS.md`
- Phases + cut lines: `18` · Pre-build accounts/lead times: `24` · Prod migration: `19`
- Schema + invariants: `02` · Ops/CI/testing/jobs: `21` · Git manifest: `23`
- Privacy (GDPR Art. 9, this app is higher-stakes than normal): `20`
