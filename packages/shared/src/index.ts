// @agbc/shared: contracts (zod, per edge function), generated database types, constants,
// and design tokens, consumed by BOTH apps/mobile and apps/dashboard (docs/spec/21 §1).
// Contracts land with their edge functions (W1.3 onward); database.types.ts lands at W0.10.
export * from './theme';
