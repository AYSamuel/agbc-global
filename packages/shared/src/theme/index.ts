// Placeholder until W0.7 lands the real token scale from docs/spec/05 (light + dark).
// Both the mobile ThemeProvider and the dashboard Tailwind config will consume THIS module
// so app and dashboard cannot drift (docs/spec/24 §2.6). No component imports raw values.
export const tokens = {} as const;
