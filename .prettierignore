# Prettier formats only our own app/package code. The spec docs, mockups, prototypes,
# and generated/vendor output are never reformatted.
docs/
supabase/
*.md
apps/mobile/assets/
# Generated; must stay byte-identical to `supabase gen types` output (CI drift gate).
packages/shared/src/database.types.ts
# Rewritten by the frameworks on every dev/build run, and gitignored. Formatting them
# only means `format:check` fails the next time anyone runs a build.
apps/dashboard/next-env.d.ts
apps/mobile/expo-env.d.ts
# Generated by `pnpm --filter mobile gen:map` from Natural Earth data; the generator
# output is authoritative, so prettier never rewraps its 50KB of coordinates.
apps/mobile/src/features/family/world-land.ts
node_modules/
.next/
.expo/
dist/
coverage/
pnpm-lock.yaml
*.hbc
# Machine-written by Claude Code (permissions cache); never hand-formatted.
.claude/settings.local.json
